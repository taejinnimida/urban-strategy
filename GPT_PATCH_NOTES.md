# GPT PATCH NOTES — 사업구역 통합폴리곤 보정

기준본: `urban-strategy-v2.5.0-r32-project-streetblock-fact-split.zip`
앱 내부 버전: `2.5.0` 유지

## 사용자 확인사항
- 카드 제목 **`사업구역·가로구역 검토`는 그대로 유지**한다.
- 이 화면은 특정 사업을 먼저 정하는 판정화면이 아니라 **공간 FACT 검증 화면**이다.
- r32에서 가로구역 자체는 실제 사례에서 1개·복수(11개 포함)로 안정적으로 분리되는 것을 확인했다.
- 남은 문제는 가로구역만 분리한 뒤, **가로구역과 가로구역 사이의 가로망/분할시설까지 다시 합친 상위 `사업구역` 통합폴리곤**을 별도 구조로 만들지 않았다는 점이다.
- 쿨데삭형 내부 가로망은 가로구역 면에서는 홈처럼 빠질 수 있으나, 상위 사업구역계에서는 그 가로망을 다시 포함하여 **닫힌 외곽 폴리곤**이 되어야 한다.

## 수정 원칙
`검토요청지 → separator FACT → 가로구역 후보 분리 → 가로구역 + 사이 가로망/분할시설 재통합 → 사업구역 통합폴리곤`

가로구역 추출 로직은 유지하고, 그 위 단계의 사업구역 생성만 보강한다.

## 수정 내용
### 1. 상위 사업구역 통합폴리곤 생성
- `buildProjectStreetBlockValidation()`에서 `rawBlocks`와 `sepUnion`을 다시 `union`한다.
- 이 통합 도형을 `projectStreetBlockValidation.projectArea`로 저장한다.
- `analysisState.metrics.project_area`도 동일 통합 도형을 사용한다.
- 사업구역의 근거를 `street_blocks_plus_internal_network`로 명시한다.

### 2. 쿨데삭 폐합
- 가로구역을 만들 때 빠진 쿨데삭 도로면은 `sepUnion`에 남아 있다.
- 상위 사업구역 생성 시 `rawBlocks + sepUnion`을 재결합하므로, 쿨데삭 홈이 사업구역 외곽을 파고들지 않고 통합면으로 닫힌다.

### 3. 검토요청지와 사업구역 분리 보존
- `requestArea`와 `projectArea`를 별도 필드로 보존한다.
- 검토요청지는 분석 프레임이며, 사업구역은 가로구역+가로망 재통합 결과다.
- Turf topology 오류로 통합면이 검토요청지의 97% 미만으로 비정상 축소될 때만 검토요청지 형상을 안전 fallback으로 사용하고 `integrationFallback`을 기록한다.

### 4. UI
- 제목은 그대로 유지한다.
- 사업구역 상태를 `가로구역+가로망 통합 · 검증용`으로 표시한다.
- 범례에 `사업구역=가로구역+사이 가로망 통합`을 추가한다.
- 붉은 2점쇄선 사업구역계를 가로구역/검토요청지 선보다 앞에 그려 실제 통합 사업구역계가 화면에서 가려지지 않게 한다.

## 이번 수정에서 건드리지 않은 것
- 가로구역 separator 판정
- 4m 기준의 현재 조작적 분할 로직
- 사업별 RULE 엔진
- 역세권·안심주택·재개발 등 기존 판정모듈
- 기존 UI 제목과 전체 배치

## 검증
- `python -m py_compile app.py regression_checks.py` PASS
- 인라인 JavaScript `node --check` PASS
- `check_spatial_evidence_maps` PASS
- `check_r14_street_block_auto` PASS
- `check_r15_street_block_4m_conditional` PASS

## 실제 대상지 확인 포인트
1. 11개 가로구역 사례: 내부 주황 가로구역 11개는 그대로 유지되어야 한다.
2. 그 사이 가로망/분할시설을 포함한 **하나의 붉은 2점쇄선 사업구역 외곽계**가 별도로 보여야 한다.
3. 쿨데삭 사례: 주황 가로구역의 홈 모양과 달리 붉은 사업구역계는 쿨데삭 도로면을 포함하여 닫힌 외곽 폴리곤이어야 한다.
4. 사업구역 면적은 `가로구역 원형 + 사이 가로망/분할시설` 통합면적을 사용한다.

## r34 — 사업구역 항등식 오류 수정
- 원인: `rawBlocks = request - allSeparators` 후 `union(rawBlocks, allSeparators)`를 수행하여 수학적으로 항상 검토요청지와 동일한 면이 생성됨.
- 추가로 통합면이 요청지의 97% 미만이면 강제로 검토요청지로 되돌리는 fallback이 있어 실제 사업구역 축소 후보가 사라졌음.
- 수정: 가로구역 분할에는 전체 separator를 쓰되, 사업구역 통합에는 `내부 가로망·분할시설`만 재포함하고 `경계부 외곽도로·외곽시설`은 제외.
- 쿨데삭/내부진입도로는 경계부 점접촉이 있어도 대부분이 내부에 있으면 내부 separator로 유지하여 사업구역 외곽면이 닫히도록 함.
- 97% 강제 fallback 삭제. 형상 생성 자체가 실패한 경우에만 검토요청지 fallback.
- UI에 내부가로망 면적과 경계부 제외면적을 구분 표시.
