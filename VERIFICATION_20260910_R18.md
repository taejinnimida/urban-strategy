# R18 검증 — 분석 파이프라인 안정화·속도개선

기준 프론트엔드: R17 `app.html`  
기준 백엔드: R15 `app.py`  
앱 자체 버전: **v2.5.0 유지**  
패치 표식: `R18_PIPELINE_STABILIZATION_20260910`

## 1. 사용자 실측 화면에서 확인된 병목

사용자 화면 총 분석시간은 **06:49(409초)**였다. 화면에 표시된 개별 단계 시간의 합은 약 **03:34(214초)**로, 약 **03:15(195초)**가 진행률에 나타나지 않았다.

코드 확인 결과 `runAllAutoAnalyses()`에는 `학교 절대보호구역` 단계가 순차 실행되고 있었으나 `ANALYSIS_PROGRESS_ORDER`에서 누락되어 있었다. 기존 학교 로더는 첫 호출 때 서울 전역 UOA110 도형을 WGS84로 변환·색인하는 구조여서 이 숨은 시간이 장시간 정지처럼 보일 수 있었다.

또한 다음 구조적 문제가 동시에 확인됐다.

- 도로·접도는 브라우저 VWorld `TL_SPRD_MANAGE` 직접조회만 사용해, 서버 가로구역이 가진 도로 Fact와 분리되어 있었다.
- R15 도로 서버는 `TL_SPRD_RW`와 `TL_SPRD_MANAGE`를 동시에 요구해, 이미 합의한 MANAGE/ROAD_BT 단일 원칙과 충돌했다.
- 역세권활성화 노선형 상업지역은 도로 0건·상업지역 0건을 실제 부재와 데이터 미확보로 구분하지 못한 채 FAIL로 내릴 수 있었다.
- 소형 Leaflet 도면은 컨테이너 크기 확정 전에 `fitBounds()`가 실행되어 대상지가 점처럼 남을 수 있었다.
- 의료시설은 대상지 분석 때마다 `TbHospitalInfo` 최대 20,000건을 1,000건 단위로 실시간 순차 재조회하고, 인근 의료시설의 대표필지도 순차 조회하고 있었다.

## 2. R18 변경

### 2.1 학교 절대보호구역

- 진행률 항목에 `학교 절대보호구역`을 명시해 숨은 단계 제거.
- ZIP 전체 서울 도형을 최초 요청 때 전부 변환·색인하지 않는다.
- 대상지 bbox를 EPSG:5174 원자료 좌표계로 변환한 뒤 `iterShapeRecords(bbox=...)`로 주변 레코드만 읽고 UOA110만 판정한다.
- 실제 19.68MB UO101 서울 ZIP을 사용한 로컬 실측(청구역 인근 약 1ha 테스트 폴리곤): **첫 호출 0.574초 / 두 번째 0.060초**. Render 실서버 시간은 배포 후 별도 실측 필요.

### 2.2 의료시설

- 사이트를 그릴 때마다 서울시 전체 병의원 20,000행을 다시 호출하지 않는다.
- 패키지의 공식 월간 `TbHospitalInfo` 스냅샷을 인터랙티브 분석의 1차 FACT 테이블로 사용하고, 스냅샷이 없을 때만 실시간 API를 대체경로로 사용한다.
- 현 패키지 스냅샷은 92개 인정후보 행이며 로컬 읽기 약 **1.9ms** 확인.
- 1.5km 내 후보의 대표필지 조회는 최대 4개 동시처리로 변경. 개별 조회 실패는 계속 `REVIEW`이며 임의 PASS로 바꾸지 않는다.

### 2.3 도로·접도 / 가로구역 공통 도로 FACT

- 서버 `/api/spatial/road-facts`를 `TL_SPRD_MANAGE + ROAD_BT` 단일 도로 FACT의 1순위로 사용.
- `TL_SPRD_RW` 필수 의존 제거.
- ROAD_BT 폭원의 절반으로 만든 중심선 버퍼면은 **가로구역·접도 계산용 개략면**일 뿐 공식 도로구역/실폭도로면으로 확정하지 않는다.
- 브라우저 VWorld WFS는 서버 도로 FACT가 없을 때만 폴백한다.
- 자료가 없으면 `REVIEW/NO_DATA` 유지.

### 2.4 역세권활성화 노선형 상업지역

- 주변 도로 0건 또는 상업지역 도형 0건만으로 FAIL 확정 금지 → `REVIEW`.
- 도로·상업지역 원자료가 실제 확보된 상태에서 후보가 없는 경우에만 FAIL.
- 도로 원자료는 가로구역/접도와 동일한 서버 MANAGE Fact를 우선 사용.

### 2.5 미니맵

- `invalidateSize()`를 먼저 수행하고 다음 tick에서 `fitBounds()` 후 다시 `invalidateSize()`하도록 순서를 변경.
- 정비구역/개발구역 등 독립 미니맵의 지연 refit도 동일 방향으로 보강.

### 2.6 보전환경

- 서로 독립적인 비오톱과 공익용산지 서버 조회를 `Promise.allSettled()`로 동시 실행.
- 하나가 실패해도 다른 하나의 FACT는 유지한다.

## 3. 회귀검증

- `python -m py_compile app.py`: PASS
- `node --check` (app.html inline JS): PASS
- R15 토지대장 targeted regression: **ALL PASS**
- R17 판정연결 targeted regression: **15/15 PASS**
- R18 신규 pipeline targeted regression: **15/15 PASS**
- FastAPI `/health`: **200** (로컬 테스트)

## 4. R18에서 바꾸지 않은 것

- 앱 버전 v2.5.0
- 장기전세/도심공공주택복합 등 R17 판정기준
- 가로주택·역세권활성화·역세권복합·성장잠재권의 사업별 가로구역 Rule
- 토지대장 R15 성능구조와 캐시
- FACT 부족 시 REVIEW/UNKNOWN 원칙

## 5. 배포 후 반드시 확인할 실측 항목

R18은 프론트엔드와 백엔드를 모두 교체해야 한다. 배포 후 같은 청구역 대상지를 다시 그려 다음을 비교한다.

1. 총 06:49가 얼마나 단축되는지
2. `학교 절대보호구역`이 별도 진행항목으로 표시되고 장시간 정지하지 않는지
3. `도로·접도`가 서버 MANAGE/ROAD_BT Fact를 확보하는지
4. 노선형 상업지역에서 자료 0건이 FAIL이 아닌 REVIEW인지
5. 정비구역·개발구역·문화재 등 미니맵이 대상지 축척으로 정상 표시되는지
6. 의료시설 단계가 매 분석마다 서울 전체 API를 재호출하지 않는지

정비사업 GIS·개발사업 GIS 등 외부/대형 공간자료 단계의 실서버 지연은 이번 로컬 정적검증만으로 개선폭을 확정하지 않는다.
