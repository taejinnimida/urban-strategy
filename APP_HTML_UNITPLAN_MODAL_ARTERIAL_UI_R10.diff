--- app.before_r10.html	2026-09-20 05:33:22.677035275 +0000
+++ app.html	2026-09-20 05:33:24.275199566 +0000
@@ -2024,7 +2024,7 @@
 <div class="spatial-mini-map" id="ccDevelopmentStatusMiniMap"></div>
 </section>
 <section class="spatial-module site-analysis-card site-analysis-local" id="siteDetail_districtUnitPlan">
-<div class="spatial-module-head"><b>지구단위계획 / 현재 계획기준</b><span id="spDistrictUnitPlanState">구역 설정 전</span></div>
+<div class="spatial-module-head"><b>지구단위계획 / 현재 계획기준</b><div class="spatial-module-head-actions"><span id="spDistrictUnitPlanState">구역 설정 전</span><button class="spatial-mini-action" type="button" onclick="openDistrictUnitPlanReviewModal()">상세검토</button></div></div>
 <div class="spatial-metrics">
 <div><span>지구단위계획구역</span><b id="spDistrictUnitPlanZone">-</b></div>
 <div><span>대상지 중첩률</span><b id="spDistrictUnitPlanOverlap">-</b></div>
@@ -2197,9 +2197,9 @@
 <div class="spatial-caption">도면정보 · 검은선은 대상지, 주황면은 <b>판정모형용 노선형 상업지역 참조도형</b>, 진한 주황은 대상지 편입부, 붉은선은 주변 도로 문맥입니다. 가로구역 자동분석값이 확보되면 동일 도면에 보라색 점선으로 함께 표시합니다.</div>
 <div class="spatial-caption"><b>판정 원칙</b> · 역세권활성화 간선도로변 유형은 <b>역세권 외 노선형 상업지역을 포함하는 가로구역 전체 또는 일부</b>를 검토합니다. 여기의 주황면은 서울시 공식 법정도형이 아니라 <b>정비사업플랫폼 내부 판정모형용 참조도형</b>이며, 일반 용도지역 FACT를 대체하지 않습니다. 본 참조도형과 대상지·역세권활성화 전용 가로구역의 중첩관계만 간선가로형 판정에 사용합니다.</div>
 <div class="spatial-mini-map" id="ccActivationArterialMiniMap"></div>
-<div class="spatial-module-head" style="margin-top:10px"><b>역세권 특례제도 간선도로 검토</b><span>폭 기준 5개 제도 독립판정</span></div>
+<div class="spatial-module-head" style="margin-top:10px"><b>역세권 특례제도 간선도로 검토</b><span>6개 제도 경로 비교 · 활성화는 노선형 상업지역 경로</span></div>
 <div class="station-scheme-list" id="widthRoadSchemeSummary"><div class="station-scheme-row head"><b>사업방식</b><span>판정</span><span>간선도로 근거</span></div><div class="station-scheme-row"><b>분석 전</b><span class="scheme-tag INFO">-</span><span>구역계를 설정하고 검토하기를 눌러야 계산됩니다.</span></div></div>
-<div class="spatial-caption">이 표는 접도현황 엔진이 이미 확보한 <b>도로 폭원·접면 공간 Fact</b>를 제도별 수치기준으로 즉시 판정합니다. 도로법상 특별시도·주/보조간선 위계, 간선도로 교차지 200m, 6m 도로 폐합처럼 별도 공간·법정 정의가 필요한 항목은 각 사업의 독립 Rule Module에서 따로 판정하며 이 표의 폭원·접면 결과를 확인필요로 되돌리지 않습니다. 역세권활성화 간선가로형은 도로폭이 아니라 위 노선형 상업지역 편입 여부로 별도 판정합니다.</div>
+<div class="spatial-caption">이 표는 역세권 특례제도의 간선도로 관련 경로를 한곳에서 비교합니다. 안심주택·성장잠재권·장기전세·도심복합 유형은 접도현황 엔진의 <b>도로 폭원·접면 공간 Fact</b>를 사용하고, <b>역세권활성화 간선가로형은 도로폭 기준이 아니라 위 노선형 상업지역 참조도형과 역세권활성화 전용 가로구역의 관계</b>를 그대로 표시합니다. 도로위계·간선도로 교차지·6m 도로 폐합 등 별도 법정 정의는 각 독립 Rule Module의 판정을 유지합니다.</div>
 </section>
 <section class="spatial-module site-analysis-card site-analysis-context" id="siteDetail_center">
 <div class="spatial-module-head"><b>중심지</b><span>서울 중심지체계</span></div>
@@ -5284,7 +5284,15 @@
 // 이 영역은 기존 Fact를 분류해 표시할 뿐 사업 판정식을 새로 만들지 않는다.
 const URBAN_RENEWAL_FRONTAGE_SCHEME_KEYS=['redevelopment','residential_environment'];
 const STATION_SPECIAL_FRONTAGE_SCHEME_KEYS=['activation','station_complex','public_complex'];
-const WIDTH_ROAD_SCHEME_KEYS=['safe','growth','longterm','innovation_growth','innovation_housing'];
+const WIDTH_ROAD_SCHEME_KEYS=['activation_arterial','safe','growth','longterm','innovation_growth','innovation_housing'];
+function activationArterialSummaryFact(){
+  const a=activationArterialAnalysis||{},loaded=a.loaded===true;
+  const status=!loaded?'REVIEW':a.routeStatus==='PASS'?'PASS':a.routeStatus==='FAIL'?'FAIL':'REVIEW';
+  const siteRel=a.siteIncludes===true?'대상지 참조도형 포함':a.siteIncludes===false?'대상지 참조도형 미포함':'대상지 관계 확인필요';
+  const blockRel=a.blockIncludes===true?'가로구역 포함':a.blockIncludes===false?'가로구역 미포함':'가로구역 연계 대기';
+  const refCount=Number(a.commercialZones?.length||0);
+  return {fact_key:'activationArterialRouteFact',key:'activation_arterial',label:'역세권활성화(간선가로형)',metric_type:'linear_commercial_route',status,value:loaded?`${siteRel} · ${blockRel} · 참조도형 ${refCount}건`:'노선형 상업지역 분석 전',criterion:'역세권 외 노선형 상업지역을 포함하는 역세권활성화 전용 가로구역 경로',contact_value:'도로폭 독립',note:a.path||'도로폭 수치기준이 아니라 노선형 상업지역 참조도형·전용 가로구역 관계로 판정'};
+}
 function roadSummaryStatusMeta(status,completedLabel='충족'){
   if(status==='CONFIRMED'||status==='PASS')return {cls:'PASS',label:completedLabel};
   if(status==='FAIL')return {cls:'FAIL',label:'미충족'};
@@ -5319,7 +5327,8 @@
   renderSchemeSummaryTable('urbanRenewalFrontageSummary',URBAN_RENEWAL_FRONTAGE_SCHEME_KEYS,facts,'접도율','충족');
   renderSmallscaleBlockSummary();
   renderSchemeSummaryTable('frontageSchemeSummary',STATION_SPECIAL_FRONTAGE_SCHEME_KEYS,facts,'접도');
-  renderSchemeSummaryTable('widthRoadSchemeSummary',WIDTH_ROAD_SCHEME_KEYS,facts,'간선도로');
+  const widthFacts={...facts,activation_arterial:activationArterialSummaryFact()};
+  renderSchemeSummaryTable('widthRoadSchemeSummary',WIDTH_ROAD_SCHEME_KEYS,widthFacts,'간선도로');
 }
 function renderSchemeRoadEvidence(){
   const facts=latestSiteFactStore?.site?.spatial_evidence?.roads||schemeRoadEvidenceFacts(),frontageFacts=latestSiteFactStore?.site?.spatial_evidence?.frontage||schemeFrontageEvidenceFacts(),selected=document.getElementById('spRoadRuleSelect')?.value||'redevelopment',rule=facts[selected]||facts.redevelopment||facts.safe,frontage=frontageFacts[selected]||frontageFacts.redevelopment;
@@ -7008,12 +7017,43 @@
   }
   try{const b=ccDistrictUnitPlanBoundary.getBounds();if(b.isValid())ccDistrictUnitPlanMini.fitBounds(b.pad(.12),{animate:false,maxZoom:18});setTimeout(()=>ccDistrictUnitPlanMini.invalidateSize(false),0);}catch(e){}
 }
-function districtUnitPlanReviewNoteHtml(){
-  const f=latestSiteFactStore?.site?.current_plan||districtUnitPlanCurrentFacts();
-  if(!f.known)return `<div class="scheme-modal-note district-unit-plan-review-note"><b>지구단위계획 / 현재 계획기준</b> 공간자료 미확보 또는 조회실패 · 사업판정에는 영향을 주지 않으며 공식 결정도서 확인이 필요합니다.</div>`;
-  if(!f.zone_overlap)return `<div class="scheme-modal-note district-unit-plan-review-note"><b>지구단위계획 / 현재 계획기준</b> 현재 확보된 공간자료에서 대상지와 지구단위계획구역 중첩이 확인되지 않았습니다. 이 항목은 사업 PASS/FAIL/REVIEW를 변경하지 않습니다.</div>`;
+function districtUnitPlanReviewNoteHtml(schemeName=''){
+  const f=latestSiteFactStore?.site?.current_plan||districtUnitPlanCurrentFacts(),safeScheme=/^[a-z0-9_]+$/i.test(String(schemeName||''))?String(schemeName):'';
+  const detailLink=`<a class="source-link" href="#" onclick="event.preventDefault();openDistrictUnitPlanReviewModal('${safeScheme}')">지구단위계획 상세검토</a>`;
+  if(!f.known)return `<div class="scheme-modal-note district-unit-plan-review-note"><b>지구단위계획 / 현재 계획기준</b> 공간자료 미확보 또는 조회실패 · 사업판정에는 영향을 주지 않으며 공식 결정도서 확인이 필요합니다. ${detailLink}</div>`;
+  if(!f.zone_overlap)return `<div class="scheme-modal-note district-unit-plan-review-note"><b>지구단위계획 / 현재 계획기준</b> 현재 확보된 공간자료에서 대상지와 지구단위계획구역 중첩이 확인되지 않았습니다. 이 항목은 사업 PASS/FAIL/REVIEW를 변경하지 않습니다. ${detailLink}</div>`;
   const names=f.zones.slice(0,3).map(x=>x.name).join(' · '),items=(f.review_items||[]).slice(0,3).join(' · ');
-  return `<div class="scheme-modal-note district-unit-plan-review-note"><b>지구단위계획 / 현재 계획기준 · REVIEW</b> ${escHtml(names||'지구단위계획구역 중첩')}. 현행계획은 사업배제조건으로 자동사용하지 않습니다.${items?` 검토사항: ${escHtml(items)}.`:''} <a class="source-link" href="${DISTRICT_UNIT_PLAN_LINKS.plan}" target="_blank" rel="noopener">공식정보</a> · <a class="source-link" href="${DISTRICT_UNIT_PLAN_LINKS.announcement}" target="_blank" rel="noopener">결정고시</a></div>`;
+  return `<div class="scheme-modal-note district-unit-plan-review-note"><b>지구단위계획 / 현재 계획기준 · REVIEW</b> ${escHtml(names||'지구단위계획구역 중첩')}. 현행계획은 사업배제조건으로 자동사용하지 않습니다.${items?` 검토사항: ${escHtml(items)}.`:''} ${detailLink} · <a class="source-link" href="${DISTRICT_UNIT_PLAN_LINKS.plan}" target="_blank" rel="noopener">공식정보</a> · <a class="source-link" href="${DISTRICT_UNIT_PLAN_LINKS.announcement}" target="_blank" rel="noopener">결정고시</a></div>`;
+}
+
+function districtUnitPlanReviewStatusMeta(f){
+  if(!f?.known)return {cls:'REVIEW',label:'자료 확인 필요'};
+  if(!f.zone_overlap)return {cls:'PASS',label:'현재 중첩 없음'};
+  return {cls:'REVIEW',label:'CURRENT PLAN 검토'};
+}
+function renderDistrictUnitPlanReviewModal(schemeName=''){
+  const body=document.getElementById('districtUnitPlanReviewBody'),title=document.getElementById('districtUnitPlanReviewTitle');if(!body)return;
+  const f=latestSiteFactStore?.site?.current_plan||districtUnitPlanCurrentFacts(),meta=districtUnitPlanReviewStatusMeta(f),scheme=/^[a-z0-9_]+$/i.test(String(schemeName||''))?String(schemeName):'';
+  const schemeLabel=scheme?(schemeNames[scheme]||scheme):'공통 현황검토',schemeRes=scheme?(schemeResults[scheme]||latestSchemeModuleResults[scheme]):null;
+  if(title)title.textContent=`지구단위계획 검토 · ${schemeLabel}`;
+  const zoneRows=(f.zones||[]).length?(f.zones||[]).map(z=>`<tr><td>${escHtml(z.name||'-')}</td><td>${z.overlap_area_m2==null?'-':Number(z.overlap_area_m2).toLocaleString('ko-KR',{maximumFractionDigits:0})+'㎡'}</td><td>${z.overlap_pct==null?'-':Number(z.overlap_pct).toFixed(1)+'%'}</td><td>${escHtml(z.layer||'UQ161')}</td></tr>`).join(''):'<tr><td colspan="4">현재 확보된 공간자료에서 중첩 구역 없음</td></tr>';
+  const ref=f.official_reference||{},refRows=(ref.matches||[]).length?(ref.matches||[]).map(m=>`<tr><td>${escHtml(m.region_name||m.location_name||m.category||'공식 조서')}</td><td>${escHtml(m.location_name||'-')}</td><td>${escHtml(m.area_after_change||'-')}</td><td>${escHtml(m.announcement_code||'-')}</td></tr>`).join(''):`<tr><td colspan="4">${escHtml(ref.message||'공식 조서 연결정보 없음')}</td></tr>`;
+  const detailRows=(f.detailed_plan_fields||[]).map(x=>`<tr><td>${escHtml(x)}</td><td>결정도서·시행지침 확인</td><td><span class="scheme-tag REVIEW">자료 미연계</span></td></tr>`).join('');
+  const reviewRows=(f.review_items||[]).length?(f.review_items||[]).map((x,i)=>`<tr><td>${i+1}</td><td>${escHtml(x)}</td><td><span class="scheme-tag REVIEW">전문가 검토</span></td></tr>`).join(''):'<tr><td colspan="3">현재 별도 REVIEW 항목 없음</td></tr>';
+  const schemeStatus=schemeRes?.overall||schemeRes?.status||'-';
+  body.innerHTML=`<div class="activation-sheet">
+    <div class="activation-sheet-title"><div><h3>CURRENT PLAN · 현행 지구단위계획</h3><p>현재 적용되는 계획정보를 수집·대조하며, 사업 가능 여부를 이 창에서 자동확정하지 않습니다.</p></div><div class="activation-feasibility"><span>현행계획 상태</span><b class="${meta.cls}">${escHtml(meta.label)}</b></div></div>
+    <section class="activation-section"><h4>1. 지구단위계획구역 공간현황</h4><table class="activation-table"><thead><tr><th>구역명</th><th>중첩면적</th><th>대상지 중첩률</th><th>공간자료</th></tr></thead><tbody>${zoneRows}</tbody></table></section>
+    <section class="activation-section"><h4>2. 공식 참조정보</h4><table class="activation-table"><thead><tr><th>공식 조서</th><th>위치</th><th>변경 후 면적</th><th>결정고시 관리코드</th></tr></thead><tbody>${refRows}</tbody></table><div class="district-plan-source-links"><a href="${DISTRICT_UNIT_PLAN_LINKS.uq161}" target="_blank" rel="noopener">UQ161 공식 공간자료</a><a href="${DISTRICT_UNIT_PLAN_LINKS.uq165}" target="_blank" rel="noopener">UQ165 특별계획구역</a><a href="${DISTRICT_UNIT_PLAN_LINKS.plan}" target="_blank" rel="noopener">공식 지구단위계획 정보</a><a href="${DISTRICT_UNIT_PLAN_LINKS.announcement}" target="_blank" rel="noopener">결정고시</a></div></section>
+    <section class="activation-section"><h4>3. 세부 결정사항</h4><table class="activation-table"><thead><tr><th>항목</th><th>현재 처리</th><th>상태</th></tr></thead><tbody>${detailRows||'<tr><td colspan="3">세부항목 목록 없음</td></tr>'}</tbody></table><div class="scheme-modal-note"><b>자동확정 금지</b> 획지·공동개발·허용/불허용도·용적률·높이·건축선·벽면선은 공식 외부 데이터가 안정적으로 연결되기 전까지 임의 생성하지 않습니다.</div></section>
+    <section class="activation-section"><h4>4. 검토사업과의 관계</h4><table class="activation-table"><tbody><tr><th>검토사업</th><td>${escHtml(schemeLabel)}</td><th>기존 사업 RESULT</th><td>${escHtml(String(schemeStatus))}</td></tr><tr><th>사업판정 영향</th><td colspan="3"><b>없음</b> · 지구단위계획 정보만으로 기존 PASS/FAIL/REVIEW를 변경하지 않음</td></tr><tr><th>차이·상충 처리</th><td colspan="3">현행계획과 사업제도상 계획값이 달라질 경우 변경·완화·의제·별도계획 적용 여부를 REVIEW ITEM으로 관리하고 기술자가 최종 판단</td></tr></tbody></table></section>
+    <section class="activation-section"><h4>5. REVIEW ITEM</h4><table class="activation-table"><thead><tr><th>No.</th><th>검토사항</th><th>처리</th></tr></thead><tbody>${reviewRows}</tbody></table></section>
+    <div class="scheme-modal-note"><b>도면 확인</b> UQ161 공간중첩 도면은 사이트 분석의 「지구단위계획 / 현재 계획기준」 카드에서 확인합니다. 특별계획구역 UQ165와 세부 계획객체는 공식 공간자료 연계가 완료되기 전까지 UNKNOWN/자료 미연계로 유지합니다.</div>
+  </div>`;
+}
+function openDistrictUnitPlanReviewModal(schemeName=''){
+  renderDistrictUnitPlanReviewModal(schemeName);
+  openReviewModal('districtUnitPlanReviewModal');
 }
 
 function renderPlanningFacilitiesImmediate(){
@@ -17533,6 +17573,16 @@
       <div class="review-modal-foot"><span>외부 서비스 화면에는 요약값만 표시하고, 검증이 필요한 상세자료는 이 창에서 확인합니다.</span><button class="btn" onclick="closeReviewModal('siteOverviewModal')">닫기</button></div>
     </section>
   </div>
+  <div class="review-modal-backdrop" id="districtUnitPlanReviewModal" aria-hidden="true">
+    <section class="review-modal review-modal-report" role="dialog" aria-modal="true" aria-labelledby="districtUnitPlanReviewTitle">
+      <div class="review-modal-head">
+        <div><h2 id="districtUnitPlanReviewTitle">지구단위계획 검토</h2><span>CURRENT PLAN → 사업제도 관계 → 차이·검토사항을 확인합니다. 최종 계획판단은 기술자 영역입니다.</span></div>
+        <button class="review-modal-close" type="button" onclick="closeReviewModal('districtUnitPlanReviewModal')" aria-label="닫기">×</button>
+      </div>
+      <div class="review-modal-body" id="districtUnitPlanReviewBody"></div>
+      <div class="review-modal-foot"><span>지구단위계획은 현재 적용계획 정보이며 사업배제 자동규제로 사용하지 않습니다.</span><button class="btn" onclick="closeReviewModal('districtUnitPlanReviewModal')">닫기</button></div>
+    </section>
+  </div>
   <div class="review-modal-backdrop" id="schemeDetailModal" aria-hidden="true">
     <section class="review-modal review-modal-report" role="dialog" aria-modal="true">
       <div class="review-modal-head">
@@ -19391,7 +19441,7 @@
 function appendBaseReviewDisclaimer(name){
   const body=document.getElementById('schemeDetailPopupBody');if(!body)return;
   if(!body.querySelector('.regulatory-review-note'))body.insertAdjacentHTML('beforeend',regulatoryReviewNoteHtml());
-  if(!body.querySelector('.district-unit-plan-review-note'))body.insertAdjacentHTML('beforeend',districtUnitPlanReviewNoteHtml());
+  if(!body.querySelector('.district-unit-plan-review-note'))body.insertAdjacentHTML('beforeend',districtUnitPlanReviewNoteHtml(name));
   if(!body.querySelector('.base-review-disclaimer'))body.insertAdjacentHTML('beforeend',baseReviewDisclaimerHtml(name));
 }
 function renderSchemeDetailPopup(name){
