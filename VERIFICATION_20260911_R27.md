# R27 검증보고서 — 사업추천 상태분리 + 역세권 특례 도로 Fact 표시 정합화

기준본: `urban-strategy-v2.5.0-20260910-road-rule-link-area10-r26-PATCH.zip`

## 1. 수정 목적

1. 개별 사업 Rule Module이 PASS인데 목적사업 분류가 `secondary`라는 이유만으로 메인 사업방식 카드에서 `자동판정 미충족`으로 표시되던 상태변환 오류를 수정한다.
2. 법정·운영기준 충족 여부와 추천 우선순위를 분리한다. PASS인 차순위 사업은 `대안추천`으로 유지한다.
3. `역세권 특례제도 간선도로 검토`에서 이미 접도현황 엔진이 확보한 도로 폭원·접면 공간 Fact를 불필요하게 REVIEW로 되돌리지 않는다.
4. 도로법상 도로위계, 간선도로 교차지 200m, 6m 도로 폐합 등 현재 폭원·접면 Fact만으로 확정할 수 없는 법정 속성은 각 사업의 독립 Rule Module에 남긴다.

## 2. 사업추천 상태변환 수정

### 원인

기존 `combineCandidateDecision()`은 독립 Rule이 PASS이고 목적사업이 차순위인 경우 `state='mid'`, `stage='ALTERNATIVE'`를 반환했다. 그러나 `candidateDisplayState()`가 모든 `mid` 상태를 `자동판정 미충족`으로 변환하여, 상세검토서는 PASS인데 메인 후보카드는 미충족으로 표시되는 모순이 발생했다.

### R27 처리

- `stage='ALTERNATIVE'`
- `regulatory='PASS'`
- `structural='PASS'`
- `areaGate='PASS'`

를 동시에 만족하면 다음과 같이 표시한다.

- display kind: `alternative`
- UI: `대안추천`
- 설명: `현재 입안·정량요건 충족 · 선택 목적사업의 대안으로 검토`
- CSS: 추진가능 계열(`candidate-on`)

실제 법정/운영기준 FAIL은 종전대로 `적용 곤란` 또는 해당 미충족 사유를 유지한다. R26의 최소면적 10% 근접부족 휴리스틱도 그대로 유지한다.

### 추천·순위 전달

`alternative`를 다음 흐름에서 추진가능 후보로 유지하도록 일관되게 연결했다.

- 메인 후보 카드
- 추천 순위
- Top 3
- 모바일 요약
- 우선순위 팝업
- 사업방식 비교표
- AI 종합분석 입력
- 결과 summary

정렬 우선순위는 `현재 추진 가능 > 대안추천 > 조건변경 후 가능` 순이다.

## 3. 역세권 특례제도 간선도로 검토 정합화

### 기본 원칙

이 카드의 역할을 `접도현황 엔진이 이미 계산한 도로 폭원·접면 공간 Fact의 사업별 수치판정`으로 명확히 했다.

따라서 ROAD_BT 기반 폭원, 접도면, 접면비 등 수치 Fact가 확보된 경우 단순히 `quality='ESTIMATE'`라는 이유만으로 REVIEW로 강등하지 않는다. 다만 폭원만으로 확인할 수 없는 법정 도로기능·위계는 별도 Rule에서 유지한다.

### 안심주택 간선도로 후보

- ROAD_BT 20m 이상 후보가 실제 공간검색에서 잡힌 `ESTIMATE` 양성결과는 초기검토 공간 Fact로 사용한다.
- AUTO가 아닌 상태에서 20m 후보가 하나도 없다는 음성결과는 도로경계 차이 가능성을 고려해 즉시 FAIL로 확정하지 않는다.

### 역세권 장기전세

간선도로 검토 카드에서는 이미 확보한 다음 공간기준만 표시한다.

- 폭 20m 이상 도로 존재
- 구역둘레 1/8 이상 접면

`간선도로 교차지 200m`와 `특별시도 중 주·보조간선도로 여부`는 장기전세 독립 입지 Rule에서 별도 판정한다. 즉 도로 폭원·접면 Fact 자체를 법정위계 미확인 때문에 REVIEW로 되돌리지 않는다.

### 도심복합 주거중심형

- 현 대상지 면적이 도로폭 기준 적용구간(2만~6만㎡)이 아니면 `확인필요` 대신 `비해당`으로 표시한다.
- 적용구간이면 요구 도로폭과 현재 최대 접면도로 폭을 직접 비교한다.
- `6m 이상 도로로 둘러싸인 일단의 지구` 및 법정 도로위계는 독립 Rule에서 별도 판정한다.

### AI 종합분석

도로 Fact가 존재하면서 `ESTIMATE`라는 이유만으로 `도로·접도 자료 추가 확인 필요`를 일괄 생성하던 문구를 제거했다. 실제 `NO_DATA`일 때만 미확보 항목으로 올린다.

## 4. 군자역 사례 재검증 시 기대 동작

R27 배포 후 동일 4필지 사례를 다시 검토하면:

- 개별 사업 Rule이 PASS이고 목적사업상 차순위인 사업은 `자동판정 미충족`이 아니라 `대안추천`으로 표시되어야 한다.
- 법정 최소면적, 용도지역, 배제조건 등 실제 FAIL이 있는 사업은 대안추천으로 승격되지 않아야 한다.
- 역세권 특례 간선도로 카드에서는 확보된 폭원·접면 수치결과를 PASS/FAIL 또는 비해당으로 표시하고, 법정 도로위계 등 별도 확인사항은 개별 사업 상세 Rule에서만 남아야 한다.

## 5. 검증

- `python -m py_compile app.py`: PASS
- Inline JavaScript `node --check`: PASS
- R15: PASS
- R17: 15/15 PASS
- R18: 15/15 PASS
- R19: 8/8 PASS
- R20: 10/10 PASS
- R21: 16/16 PASS
- R22: 13/13 PASS
- R23: 26/26 PASS
- R24: 18/18 PASS
- R25: 15/15 PASS
- R26: 37/37 PASS
- R27 신규: 25/25 PASS

R27 신규검사는 PASS+secondary 상태의 대안추천 변환, 추천/Top3/AI 전달, 기존 FAIL 보존, ROAD_BT ESTIMATE 양성결과 사용, 음성결과 보수적 처리, 장기전세 폭원·접면/법정위계 분리, 주거중심형 비해당 처리, AI NO_DATA 구분, R20~R26 핵심 기능 보존을 포함한다.

## 6. 보존사항

- R20 가로구역 면적 선필터 유지
- R21 4m/6m 주택접도율 유지
- R22 prepared geometry 유지
- R23 lazy neighbor graph 유지
- R24 지번 입력/지도 클릭 구역계 확정 UI 유지
- R25 역세권활성화 가로구역/접도 정합성 유지
- R26 접도 Fact 사업별 연결 및 최소면적 10% 추천필터 유지
- 내부 앱 버전 `v2.5.0` 유지
