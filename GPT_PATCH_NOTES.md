# GPT PATCH NOTES — 사업구역·가로구역 FACT 분할 보정

기준본: `urban-strategy-v2.5.0-r31-project-streetblock-rework(1).zip`
앱 내부 버전: `2.5.0` 유지

## 사용자 확인사항
- 카드 제목 **`사업구역·가로구역 검토`는 그대로 유지**한다.
- 이 화면은 특정 사업을 미리 선택하는 판정화면이 아니라 **공간 FACT를 확인하는 화면**이다.
- 실제 사례에서 검토요청지 약 17,352㎡가 도시계획시설/도로에 의해 육안상 3개 구획으로 명확히 나뉘는데, 이전 빌드는 5,791㎡ 1개 구역만 추출했다.

## 원인
1. `project_area`를 `selectedParcelPnus` 합집합에서 먼저 만들면서 검토요청지 전체가 아닌 일부 토지군만 남을 수 있었다.
2. 경계후보 필지는 `selectedParcelPnus`에서 빠지므로 그 필지가 도로인 경우 가로 분할선도 함께 누락될 수 있었다.
3. VWorld 도시계획시설에서 개별 명칭이 `기타도시시설`처럼 내려오면, 실제 조회 레이어/카테고리가 도로(`LT_C_UPISUQ151`)여도 `classifyProjectFacility()`가 `other`로 분류할 수 있었다.
4. 도로·접도 단계에서 이미 확보한 `TL_SPRD_MANAGE ROAD_BT` 도로면을 이 FACT 분할에서 재사용하지 않았다.

## 수정
### 1. project_area
- 특정 사업별 제척 가정을 적용하지 않고 **사용자가 확정한 검토요청지 전체를 project_area 후보 프레임으로 유지**한다.
- 기존 55% 경계부/1,500㎡ 단부시설 기준은 project_area를 잘라내는 데 사용하지 않는다.
- 도로·시설 관계값은 진단 FACT로만 보존한다.

### 2. 가로구역 후보 분할 입력
다음 공간 FACT를 순차적으로 separator에 사용한다.
1. 검토요청지와 교차하는 연속지적 도로필지 — 자동포함/경계후보 여부와 무관하게 `parcelFeatureMap` 전체에서 확인
2. `TL_SPRD_MANAGE ROAD_BT`로 확보한 4m 이상 현황도로면
3. 도시계획시설 도로·철도·하천·주차장·광장·공원·녹지·공공공지·학교

### 3. `기타도시시설` 오분류 방지
- 시설분류를 `name` 하나로 판단하지 않는다.
- `name + category + layer`를 함께 사용한다.
- `layer === LT_C_UPISUQ151`이면 개별 명칭이 `기타도시시설`이어도 도로로 복원한다.

### 4. UI
- 제목 `사업구역·가로구역 검토` 유지.
- 설명문은 `공간 FACT 검증용`으로 정리.
- `외곽 제척 도로` 표현은 실제 제척을 하지 않으므로 `도로 FACT`로 변경.
- 사업구역 상태는 `검토요청지 기반 · 검증용`으로 표시.
- 기존 사업 판정엔진에는 아직 연결하지 않는다.

## 검증
- `python -m py_compile app.py regression_checks.py` PASS
- `node --check` 인라인 JS PASS
- 신규/수정 가로구역 정적 회귀검사 PASS
- 도정법/소정법/역세권 도로 3그룹 회귀검사 PASS
- 전체 회귀검사는 `r9 refinement placement`까지 전부 PASS 후, 기준 ZIP에 없는 `도시계획변경 사전협상 운영지침(11차개정_2026.06.29).pdf`를 요구하는 기존 `r10 scheme fail-safe`에서 중단. 이번 수정과 무관.

## 실제 사례 재검증 포인트
- 같은 대상지를 다시 돌렸을 때 사업구역 면적이 검토요청지 전체와 일치하는지 확인.
- 도시계획시설 현황도에서 보이는 내부 도로/시설 분할선이 `가구역·나구역·다구역` 3개 후보로 복원되는지 확인.
- 예상보다 구획이 더 많거나 적으면 `분할시설` 레이어를 먼저 확인해 어떤 도로 FACT가 separator로 들어왔는지 점검한다.
