# R32 검증보고서 — 준공업지역 판정박스 하단 이동 / 역세권·공장비율 분리

## 1. 기준본
- 기준본: `urban-strategy-v2.5.0-20260911-semiindustrial-industrial-facts-r31-PATCH.zip`
- 내부 앱 버전: **v2.5.0 유지**
- 수정범위는 R31 준공업지역 모듈의 표시순서와 도심복합개발 주거중심형 역세권 판정 분리에 한정했다.

## 2. 수정내용

### 2.1 `준공업지역 정비·개발` 패널 위치
기존에는 `사업방식 적용판정` 제목 직후, 일반 사업카드보다 앞에 표시되던 전용 패널을 아래 순서의 **맨 마지막 사업군**으로 이동했다.

`주택정비 → 소규모주택정비 → 민간주택개발 → 특례 주택사업 → 도심정비사업 → 특례개발사업 → 준공업지역 정비·개발 → AI 종합분석`

기능·판정값·세부팝업은 변경하지 않고 DOM 표시순서만 조정했다.

### 2.2 도심복합개발 주거중심형 역세권 판정 분리
R31에서는 준공업지역의 `입지` 행에서 아래 두 조건을 한 행으로 묶었다.
- 대상지 전체가 역세권에 포함되는지 여부
- 법정 공장비율 10% 미만 여부

이 때문에 역세권 350m에 대상지 전체가 포함되더라도 2008.1.31 기준 법정 공장비율이 미확정이면 공간현황의 역세권 판정까지 `REVIEW`로 내려가는 문제가 있었다.

R32에서는 다음과 같이 분리했다.

1. **입지(역세권) 행**
   - 일반지역: 대상지 면적 50% 이상
   - 준공업지역: 대상지 면적 전체 포함
   - 350m 충족 → PASS
   - 350~500m 전체 포함 → 시장 인정 필요 REVIEW
   - 공장비율 값은 이 행의 status 계산에 사용하지 않음

2. **공장비율 행**
   - 준공업지역에서만 별도 하드게이트로 표시
   - 서울시 도시계획조례 별표17 2008.1.31 기준 공장비율 10% 미만
   - 값 미확정 → REVIEW
   - 10% 미만 → PASS
   - 10% 이상 → FAIL

따라서 `역세권 PASS + 공장비율 REVIEW = 사업 전체 REVIEW`는 가능하지만, `공장비율 REVIEW → 역세권 자체 REVIEW`가 되지 않도록 수정했다.

### 2.3 공간현황 역세권 요약표
도심복합 주거중심형의 경로 문구도 용도지역에 따라 동적으로 표시한다.
- 일반지역: `350/500m 대상지 면적 50% 이상`
- 준공업지역: `350/500m 대상지 전체포함`

## 3. 유지한 기능
- R31 `산업 및 공장비율` 3종 FACT
- R31 산업혁신구역 / 산업정비구역 전용 검토
- R31 VWorld `LT_C_DAMDAN` 산업단지 경계
- R31 최신 UQ181 2026-09
- R30 동의율 비게이트화 / 계획변수 `계획반영필요`
- R29 서울플랜+ 기존 추진사업 registry
- R28 브라우저 동결 방지
- R27 후보 추천상태 및 대안추천
- R26 면적부족 10% 조정 후보
- R24 지번/클릭/SHP 구역계 입력

## 4. 검증결과

### 문법검사
- `python -m py_compile app.py` → PASS
- inline JavaScript `node --check` → PASS

### 기존 회귀검사
- R15 → PASS
- R17 → 15/15 PASS
- R18 → 15/15 PASS
- R19 → 8/8 PASS
- R20 → 10/10 PASS
- R21 → 16/16 PASS
- R22 → 13/13 PASS
- R23 → 26/26 PASS
- R24 → 18/18 PASS
- R25 → 15/15 PASS
- R26 → 37/37 PASS
- R27 → 25/25 PASS
- R28 → 14/14 PASS
- R29 → 27/27 PASS
- R30 → 25/25 PASS
- R31 → 48/48 PASS

### R32 신규검사
- `targeted_regression_checks_r32.py` → **26/26 PASS**
- 전용 패널이 일반 사업군 뒤·AI 분석 앞에 위치하는지 확인
- 준공업지역 역세권 status 계산에서 `factory_pct`가 사용되지 않는지 확인
- 별도 공장비율 하드게이트가 유지되는지 확인
- 준공업/일반지역별 역세권 경로 문구가 동적으로 구분되는지 확인

## 5. 판정 원칙
- 공간 FACT(역세권)와 다른 독립 법정요건(공장비율)은 서로 상태를 오염시키지 않는다.
- 사업 전체 결과에서는 두 하드게이트를 모두 반영한다.
- 자료 미확보는 해당 FACT만 REVIEW 처리하고 다른 확정 FACT의 판정을 미루지 않는다.
