# R30 검증보고서 — 동의율 비게이트화 / 계획변수 `계획반영필요` 분리

기준본: `urban-strategy-v2.5.0-20260911-planplus-existing-projects-r29-PATCH.zip`

## 1. 사용자 확정 원칙

### 1.1 동의율
- 토지등소유자·토지면적·주민동의율 등은 **초기 사업가능성 PASS/FAIL/REVIEW 판정에 사용하지 않는다.**
- 법정 동의기준 자체는 삭제하지 않고 검토서에 **`사업추진 전제조건`**으로 표시한다.
- 동의율 미입력 또는 현재 미달을 이유로 후보사업을 탈락·보류하지 않는다.

### 1.2 사업계획으로 정하는 기준
- 계획 세대수, 계획 주택연면적, 특정 공급방식·공급비율, 설계단계 기준 등 **사업자가 후속 계획에서 반영할 수 있는 값**은 초기 대상지 적격성 하드게이트로 사용하지 않는다.
- 해당 기준 역시 삭제하지 않고 검토서에 **`계획반영필요`**로 표시한다.
- 반면 현재 대상지가 이미 갖고 있어야 하는 면적·용도지역·역세권·접도·노후도·배제지역 등 FACT는 종전대로 판정조건으로 유지한다.
- 소규모주택정비의 `기존주택수`, `기존주택 세대수`처럼 현재 건축물대장으로 확인하는 법정 현황값은 계획변수가 아니므로 그대로 판정한다.

## 2. R30 공통 판정정책

`schemeDecisionPolicyRole()`을 추가하고 `schemeRow()`에서 다음 두 상태를 공통 처리한다.

- `PREREQUISITE` → status=`INFO`, required=`false`, 표시=`사업추진 전제조건`
- `PLAN` → status=`INFO`, required=`false`, 표시=`계획반영필요`

따라서 `overallScheme()`, 후보추천, Top3, AI gap 계산에는 영향을 주지 않으면서 상세 검토서에서는 법정 기준을 계속 확인할 수 있다.

도로폭·접도면 등 이미 자동산정되는 현황 도로 FACT는 이 정책으로 비게이트화하지 않았다. 특히 `간선도로변 도로계획`이라는 항목명만을 이유로 PLAN 처리하지 않도록 하여 R27 도로 FACT 자동판정을 보존했다.

## 3. 개별 모듈 반영

### 3.1 민간 주택개발
- 계획 세대수는 사업가능성 기준에서 제거.
- 세대수는 `주택법 사업계획승인 / 건축법상 허가` 등 **후속 인허가 경로를 정하는 계획변수**로만 사용.
- 미입력 시 `계획반영필요`.

### 3.2 역세권 장기전세주택
- `100세대 이상`은 `계획반영필요`로 변경.
- 사업추진 동의요건은 `사업추진 전제조건`으로 변경.
- 동의율·계획세대수가 입지/사업가능성 결과를 낮추지 않음.

### 3.3 상생주택
- 부지면적 3,000㎡ 이상이면 현황 FACT로 규모요건 PASS.
- 3,000㎡ 미만인 경우 `공동주택 100세대 이상` 경로를 현재 세대수 미입력 때문에 REVIEW 처리하지 않고 `계획 세대수 / 계획반영필요`로 분리.

### 3.4 안심주택 저용도지역 특례
- 자연녹지·제1종일반주거의 `어르신 임대주택 100% 공급`은 현재 토지 FACT가 아니라 사업계획 조건으로 분리.
- 특별 입지경로 자체는 열어 두고 공급조건을 `계획반영필요`로 표시.
- 단, 역세권·간선도로변·의료시설, 면적, 용도지역, 노후도, 학교 절대보호구역 등 현황 FACT 판정은 유지.

### 3.5 도심복합개발 주거중심형
- 사업 후 `주택 연면적 비율`을 `계획반영필요`로 처리.
- 지정신청 동의는 공통 정책에 따라 `사업추진 전제조건`으로 비게이트화.

### 3.6 기타 사업의 동의행
- 성장잠재권, 역세권복합 지구단위계획, 도심복합개발 등 `schemeRow()`을 사용하는 동의 관련 행은 공통정책에 의해 자동으로 `사업추진 전제조건` 처리.
- 법정 동의율 수치·근거는 검토서에서 보존한다.

## 4. 화면 및 AI

- 일반 사업검토표·모바일 검토표·상세검토서에서 정책행을 삭제하지 않고 각각 `사업추진 전제조건`, `계획반영필요`로 표시.
- 기존에는 INFO행이 상세 검토서에서 제외되던 부분을 보완하여 정책행은 계속 표시.
- AI 입력에는 `planning_required`, `prerequisites`를 별도 전달.
- 이 항목들은 `gaps`에는 포함하지 않는다.

## 5. 보존사항

- R29 서울플랜+ 기존 고시·추진사업 registry 및 범례 유지.
- R28 가로구역 브라우저 멈춤 방지와 `frontend_postprocess_ms` 유지.
- R27 대안추천 상태 및 역세권 특례 도로 FACT 연결 유지.
- R26 최소면적 부족 10% 추천필터 유지.
- R24 지번 입력/지도 클릭 구역계 기능 유지.
- 내부 앱 버전 `v2.5.0` 유지.

## 6. 이번 R30에 포함하지 않은 별도 Rule

강남역 롯데칠성부지 사례에서 확인한 **역세권활성화사업의 5,000㎡ 이상 사전협상 대상지 제외 규정**은 이번 공통 정책 수정과 별개이므로 R30에서는 수정하지 않았다. 다음 패치에서 역세권활성화 운영기준 원문과 현재 `사전협상 대상여부` 판정경로를 별도로 확인한 뒤 독립 수정하는 것이 안전하다.

## 7. 검증

- `python -m py_compile app.py`: PASS
- Inline JavaScript `node --check`: PASS
- R15: PASS
- R17: 15/15 PASS
- R18: 15/15 PASS
- R19: 8/8 PASS
- R20: 10/10 PASS
- R21: 16/16 PASS
- R22: 13/13 PASS
- R23: 26/26 PASS
- R24: 18/18 PASS
- R25: 15/15 PASS
- R26: 37/37 PASS
- R27: 25/25 PASS
- R28: 14/14 PASS
- R29: 27/27 PASS
- **R30 신규: 25/25 PASS**

R30 신규검사는 다음을 포함한다.
- 동의 FAIL 입력도 `INFO / required=false / PREREQUISITE`로 변환되는지
- 계획 세대수 FAIL 입력도 `INFO / required=false / PLAN`으로 변환되는지
- 대상지 면적 FAIL은 여전히 하드게이트로 남는지
- 간선도로 현황 FAIL이 `계획반영필요`로 잘못 중립화되지 않는지
- 동의·계획행을 포함해도 실제 현황 PASS가 전체 PASS로 계산되는지
- 실제 현황 FAIL은 전체 FAIL을 유지하는지
- R24~R29 핵심 기능이 보존되는지
