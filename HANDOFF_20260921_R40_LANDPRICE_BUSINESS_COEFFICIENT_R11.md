# HANDOFF — R40 LANDPRICE / BUSINESS COEFFICIENT R11

- 기준: R10 `UNITPLAN-MODAL-ARTERIAL-UI` 직전 결과물
- 버전: v2.5.0 / R40 유지
- 성격: WIP. 기준본 승격 전 실제 대상지 확인 필요
- 작업일: 2026-09-21

## 1. 이번 작업 목적

사업방식과 무관한 **사업구역 평균공시지가**를 토지정보 공통 FACT로 독립하고, 이 FACT를 소비하는 **정비사업 사업성 보정계수**를 사업방식별 RULE로 분리한다.

이번 단계에서는 사업성 보정계수를 사업별 밀도·PASS/FAIL에 연결하지 않는다. 향후 허용용적률 FACT가 준비되면 사업별 팝업의 밀도계산이 이 값을 소비하도록 연결한다.

## 2. 토지정보 공통 FACT

`siteOfficialLandPriceFact()` 추가.

- 사업구역과 실제 교차하는 필지를 대상으로 함
- 지목 `대`만 산정
- 동일 기준연도 개별공시지가 사용
- 면적가중평균: Σ(개별공시지가 × 산정면적) / Σ(산정면적)
- 경계 일부 중첩필지: 공식 필지면적 × 사업구역 중첩비율을 초기검토 산정면적으로 사용
- 가격 확인 필지수·산정면적·면적 커버리지 함께 보존
- 사업방식에 따라 FACT 모집단을 바꾸지 않음

토지정보 UI에 다음 필드를 추가했다.

- 공시지가 기준연도
- 사업구역 평균공시지가(대)
- 공시지가 산정면적(대)
- 공시지가 확인률

기존 `/api/land/official-price-batch`를 그대로 사용하며 `app.py`는 변경하지 않았다.

## 3. 정비사업 사업성 보정계수 RULE

### 3.1 재개발

- 공시지가 보정계수 = 서울시 재개발 평균공시지가 / 사업구역 평균공시지가
- 최종 범위 1.00~2.00
- 서울시 2025 평균공시지가: 6,302,982원/㎡

### 3.2 재건축

- 사업성 보정계수 = 공시지가 보정계수 + α + β
- 공시지가 보정계수 = 서울시 재건축 평균공시지가 / 사업구역 평균공시지가
- 서울시 2025 평균공시지가: 8,043,979원/㎡
- α 대지면적 보정
  - 10,000㎡ 이하: 0.20
  - 10,000~20,000㎡ 미만: 0.20→0.10 직선보간
  - 20,000㎡ 이상: 적용대상 아님(0)
- β 세대밀도 보정
  - `가용용적률 × 대지면적 / 현황세대수`로 세대당 공급가능면적을 판단
  - 110㎡ 이상: β 미적용
  - 80㎡ 미만: 0.20
  - 80~110㎡ 미만: 0.20→0.10 직선보간
- β는 **가용용적률 FACT**가 연결될 때 자동산정한다. 허용용적률·법적상한용적률을 대신 넣지 않는다.
- 최종 범위 1.00~2.00

### 3.3 소규모정비

모아주택 사업성 보정계수 세부기준의 직접 적용경로를 분리했다.

- 자율주택정비
- 가로주택정비
- 소규모재개발

위 사업은 서울시 재개발 평균공시지가를 비교기준으로 사용하며:

- 계수 = 서울시 재개발 평균공시지가 / 사업시행구역 평균공시지가
- 범위 1.00~1.50

소규모재건축은 모아주택 세부기준의 직접 적용대상과 구분하여 **별도 시장 세부기준 연결 전 REVIEW**로 유지했다. 동일 산식을 임의 복제하지 않았다.

## 4. 향후 허용용적률 연결 산식

`businessCoefficientAdjustedAllowedFar()`를 추가했으나 현재 밀도계산에는 호출하지 않는다.

도정법 재개발·재건축의 향후 연결식:

`보정 허용용적률 = 기준용적률 + ((기존 허용용적률 - 기준용적률) × 사업성 보정계수)`

- 입력 FACT: 기준용적률, 기존 허용용적률, 사업성 보정계수
- 상한용적률·법적상한용적률 ceiling 처리는 후속 밀도모듈에서 별도
- 현재 `density_connected:false`

## 5. 기존 기능 보존

다음 핵심 함수 해시는 R10과 동일함.

- `buildSiteFactStore()`
- `densityForScheme()`
- `runAllSchemeChecks()`
- `checkActivationFromFacts()`
- `analyzeSchemeStreetBlocks()`
- `analyzeRoadAccess()`
- `runAllAutoAnalyses()`

R10 지구단위계획 CURRENT PLAN / 상세검토 modal, 역세권활성화 간선가로형 UI, R8 준공업, R7 용도지역 서버조회 등 기존 기능은 변경하지 않았다.

## 6. 후속 작업

1. 실제 대상지를 이용해 지목 `대` 필지 모집단과 평균공시지가를 화면에서 검증
2. 재건축 가용용적률 FACT 연결 후 β 자동산정 검증
3. 소규모재건축의 별도 서울시 세부기준 확정·연결
4. 사업별 기준·허용용적률 FACT 구축 완료 후 `businessCoefficientAdjustedAllowedFar()`를 각 사업 팝업 밀도모듈에 연결
5. 상한·법적상한 및 공공기여와 결합한 최종 계획가능용적률 산식은 별도 단계에서 구현

