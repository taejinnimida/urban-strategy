# v2.5.0 R38 접도면 기하분리·누적 폭원 판정 검증

기준본: `urban-strategy-v2.5.0-20260914-hillside-terrain-fact-r37-PATCH.zip`

## 1. 수정 목적

R36 접도도면에서 서로 다른 대상지 경계면이 시각적으로 분리되는데도, 기존 `siteRoadNetworkStats()`는 도로명/RN_CD 단위 그룹 수를 접도면수로 사용했다. 따라서 같은 도로명이 대상지의 서로 다른 방향 경계에 접하거나 한 도로명이 여러 폭원 관리구간을 가지는 경우 `4m+ / 6m+ / 8m+` 면수가 모두 1면으로 축약될 수 있었다.

R38은 **도로명 단위 도로 Fact와 대상지 경계의 독립 접도면 Fact를 분리**하고, 사업 판정용 접도면수를 기하학적 접도면 기준으로 계산한다.

## 2. FACT 구조

- `roads`: 기존 도로명/RN_CD 그룹 유지. 도심복합 성장거점형의 "20m 도로 + 다른 8m 도로"처럼 서로 다른 실제 도로를 구분하는 사업별 Fact에 사용한다.
- `frontageFaces`: 대상지 경계에서 ROAD_BT 도로면과 실제로 접하는 연속구간을 기하적으로 분리한 독립 접도면 목록이다.
- 접도면 분리 알고리즘
  - R36의 0.5m 경계 샘플링 유지
  - 경계 진행방향이 45° 이상 꺾이는 지점에서 접도구간을 분리
  - 1.25m 이내의 미세 공백이고 방향차가 30° 이내인 조각은 동일 면으로 병합
  - 폴리곤 링 시작점 때문에 같은 직선면이 앞/뒤로 끊긴 경우 순환 병합
- 위 각도/거리값은 **법적 숫자기준이 아니라 접도면 형상 정리용 분석 파라미터**다.

## 3. 누적 폭원 판정

도면 색상은 서로 겹치지 않는 배타적 구간으로 표시한다.

- 35m 이상
- 20m 이상 ~ 35m 미만
- 8m 이상 ~ 20m 미만
- 6m 이상 ~ 8m 미만
- 4m 이상 ~ 6m 미만
- 4m 미만

사업 판정용 면수는 배타구간이 아니라 실제 폭원에 따라 **누적** 계산한다.

- `road4Faces`: 폭 4m 이상 접도면 전부
- `road6Faces`: 폭 6m 이상 접도면 전부
- `road8Faces`: 폭 8m 이상 접도면 전부
- `road20Faces`: 폭 20m 이상 접도면 전부
- `road35Faces`: 폭 35m 이상 접도면 전부

따라서 34m 도로는 4/6/8/20m 기준에 모두 포함되고, 40m 도로는 4/6/8/20/35m 기준에 모두 포함된다. **25m 별도 판정 임계값은 추가하지 않았다.** 10m·15m처럼 일부 제도에서 별도 사용하는 폭원은 각 접도면/도로의 실제 폭원값을 사업별 RULE이 직접 읽도록 기존 구조를 유지한다.

## 4. UI 변경

`사이트 분석 > 접도현황`

- `독립 접도도로` → `독립 접도면`
- 누적 면수에 `20m 이상`, `35m 이상` 추가
- 누적 접도연장에 `20m+`, `35m+` 추가
- 표를 도로명 그룹이 아니라 `F1`, `F2` 등 독립 접도면 기준으로 표시
- 레전드: `35m+ / 20~35m / 8~20m / 6~8m / 4~6m / 4m 미만`
- 지목 도로 접면후보(주황 점선)는 계속 진단전용이며 판정에 미반영

## 5. 보존 항목

- 역세권활성화 RULE: `4m 이상 2면 + 8m 이상 1면` 자체는 변경하지 않음
- 도로중심선 `TL_SPRD_MANAGE + ROAD_BT` 원자료 및 도로면 생성방식 유지
- 도로명 그룹 `roads` 및 도심복합 성장거점형의 서로 다른 도로 판정 유지
- R35 노선형 상업지역 MODEL_REFERENCE 유지
- R36 지목도로 진단 레이어 유지
- R37 구릉지 FACT/도면/백엔드 파일 전부 유지
- 앱 내부 버전 `v2.5.0` 유지

## 6. 동적 단위검증

동일 도로명을 가진 두 접도구간을 인공 구성하여 기하분리/누적계산을 검증했다.

1. 서로 90°인 `34m 1면 + 6m 1면`
   - 독립 접도면 2면
   - 4m+ 2면
   - 6m+ 2면
   - 8m+ 1면
   - 20m+ 1면
   - 35m+ 0면

2. 동일 직선상의 `34m 구간 + 6m 구간`
   - 독립 접도면 1면으로 병합
   - 4m+ 1면 / 6m+ 1면 / 8m+ 1면 / 20m+ 1면 / 35m+ 0면

3. `40m 1면`
   - 4/6/8/20/35m 기준 모두 1면

## 7. 회귀검사

- R35: 33/33 PASS
- R36: 24/24 PASS (R38의 의도적 접도면 산식 업그레이드를 허용하도록 회귀조건 갱신)
- R37: 33/33 PASS
- R38: 30/30 PASS
- inline JavaScript syntax PASS
- backend Python syntax PASS

## 8. 실제 대상지 확인 필요

이번 패치는 코드 구조와 합성 기하 단위테스트까지 검증했다. 오류동역 인근 실제 구역계는 배포 환경에서 동일 경계를 다시 선택해 `4m+ / 6m+ / 8m+ / 20m+ / 35m+` 면수와 도면의 F1/F2 분리가 일치하는지 최종 현장형 회귀확인이 필요하다. 지목상 도로 후보만 있고 ROAD_BT 인정선이 없는 경우는 여전히 접도엔진 원자료 결합 문제로 별도 보정대상이다.
