# R24 검증보고서 — 지번으로 찾기(입력하기/클릭하기)

## 기준본
- 기준본: `urban-strategy-v2.5.0-20260910-streetblock-neighborgraph-r23-PATCH.zip`
- 내부 앱 버전은 `v2.5.0` 유지.
- 이번 수정은 메인 대상지 입력 UI와 필지선택 인터랙션만 변경하며, 사업별 Rule·접도율·가로구역·성능개선 백엔드는 변경하지 않음.

## 변경사항
1. 상위 입력 탭 `지번으로 입력하기` → `지번으로 찾기`.
2. `지번으로 찾기` 내부를 `입력하기 / 클릭하기` 2개 방식으로 분리.
3. 입력하기: 기존 서울 지번주소 검색 결과를 `parcelFeatureMap`과 `selectedParcelPnus`에 누적.
4. 클릭하기: 메인 지도 빈 위치 클릭 시 소규모 반경으로 VWorld 연속지적을 조회하고 클릭점을 포함하는 필지를 찾아 선택.
5. 이미 로드된 필지는 지도 클릭으로 선택/해제 토글. 레이어 클릭 이벤트는 map click으로 중복 전파되지 않도록 차단.
6. 두 방식은 동일 `selectedParcelPnus`를 공유하므로 혼합 사용 가능.
7. 공통 하단에 `선택 필지 수 / 합계면적 / 선택필지로 구역계 확정 / 선택필지 보기 / 선택 해제` 제공.
8. 선택필지 확정은 기존 `applySelectedParcelsAsBoundary()`의 지적경계 union을 그대로 사용.
9. union 결과가 여러 MultiPolygon component이면 비연속 경고를 표시하되 사용자가 계속 진행 가능.
10. 클릭 모드 진입 시 지적 표시레이어를 자동 ON하고 커서를 crosshair로 변경.

## 검증
- `python -m py_compile app.py`: PASS
- HTML inline JavaScript `node --check`: PASS
- R15 targeted regression: PASS
- R17: 15/15 PASS
- R18: 15/15 PASS
- R19: 8/8 PASS
- R20: 10/10 PASS
- R21: 16/16 PASS
- R22: 13/13 PASS
- R23: 26/26 PASS
- R24 신규: 18/18 PASS

## R24 신규검사 핵심
- 탭 명칭 및 하위 2모드 존재
- 입력/클릭이 동일 선택집합 사용
- 지도 point → 필지 조회 함수 존재
- 기존 필지 클릭 중복 이벤트 차단
- 선택 필지 수·면적 표시
- 비연속 필지 경고
- R21 접도율, R20 면적 선필터, R23 neighbor graph 유지

## 운영 메모
- 지도 클릭은 법적 경계를 새로 생성하지 않고 VWorld `LP_PA_CBND_BUBUN` 필지 도형을 선택하는 입력수단임.
- 합계면적은 선택 즉시 화면반응을 위해 공식 토지대장 면적이 있으면 우선 사용하고, 없으면 연속지적 도형면적을 임시 표시함. 전체 검토 실행 후 기존 토지대장 Fact로 보정됨.
- 클릭 선택 자체는 전체 사업분석을 실행하지 않으며 `선택필지로 구역계 확정` 후 사용자가 `검토하기`를 눌러 기존 전체 파이프라인을 실행함.
