--- app.before_competition.html	2026-09-18 04:29:24.359386729 +0000
+++ app.html	2026-09-18 04:30:51.192751438 +0000
@@ -1308,6 +1308,19 @@
 .ai-comprehensive-line{font-size:9px;line-height:1.55;color:#344054}.ai-comprehensive-line:before{content:'·';font-weight:900;margin-right:5px;color:#175cd3}
 .ai-comprehensive-loading{font-size:9px;line-height:1.55;color:#667085}.ai-comprehensive-foot{display:flex;justify-content:space-between;gap:8px;margin-top:6px;font-size:7px;line-height:1.45;color:#667085}
 .ai-comprehensive-foot b{color:#475467}.ai-comprehensive-foot button{border:1px solid #d0d5dd;background:#fff;color:#344054;font-size:7px;padding:3px 6px;cursor:pointer}
+
+/* v2.5.0 r40 사업성 제고제도 경합 표시: 역세권활성화 ↔ 사전협상 */
+.scheme-competition-panel{display:none;margin:7px 0 2px;border:1px solid #fda29b;background:#fff8f7;border-radius:9px;overflow:hidden}
+.scheme-competition-panel.active{display:block}
+.scheme-competition-head{display:flex;align-items:flex-start;justify-content:space-between;gap:10px;padding:8px 10px;border-bottom:1px solid #fecdca;background:#fef3f2}
+.scheme-competition-head b{display:block;font-size:11px;color:#912018}.scheme-competition-head span{display:block;margin-top:2px;font-size:7.5px;line-height:1.45;color:#7a271a}.scheme-competition-head em{font-style:normal;font-size:7.5px;font-weight:900;color:#b42318;border:1px solid #fda29b;background:#fff;padding:3px 6px;border-radius:999px;white-space:nowrap}
+.scheme-competition-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:6px;padding:7px}
+.scheme-competition-card{appearance:none;text-align:left;border:1px solid #fda29b;background:#fff;border-radius:7px;padding:7px 8px;cursor:pointer;color:#7a271a}
+.scheme-competition-card strong{display:flex;align-items:center;justify-content:space-between;gap:6px;font-size:10px;color:#912018}.scheme-competition-card strong em{font-style:normal;font-size:7px;padding:2px 5px;border-radius:999px;background:#fef3f2;color:#b42318}
+.scheme-competition-card span{display:block;margin-top:4px;font-size:8px;line-height:1.45;color:#475467}.scheme-competition-card small{display:block;margin-top:4px;font-size:7.2px;line-height:1.4;color:#667085}
+.scheme-competition-foot{padding:0 9px 8px;font-size:7.5px;line-height:1.5;color:#667085}.scheme-competition-foot b{color:#912018}
+@media(max-width:760px){.scheme-competition-grid{grid-template-columns:1fr}}
+
 @media(max-width:900px){.cc-candidate-head{align-items:flex-start;flex-wrap:wrap}.candidate-color-legend{margin-left:0}.ai-comprehensive-head{flex-direction:column}.ai-comprehensive-badge{align-self:flex-start}}
 .density-basis-badge{background:#eff8ff!important;border-color:#b2ddff!important;color:#1849a9!important}
 
@@ -1728,6 +1741,14 @@
   <span class="candidate-legend-note">색이 진할수록 계획가능용적률이 높음</span>
 </div>
 </div>
+<section class="scheme-competition-panel" id="schemeCompetitionPanel" aria-live="polite">
+  <div class="scheme-competition-head"><div><b>사업성 제고제도 경합</b><span id="schemeCompetitionSummary">5,000㎡ 경계에서 역세권활성화와 사전협상의 적용관계를 비교합니다.</span></div><em>전략 검토</em></div>
+  <div class="scheme-competition-grid">
+    <button class="scheme-competition-card" type="button" onclick="showCandidateBasis('activation')"><strong>역세권활성화 <em id="schemeCompetitionActivationBadge">경합검토</em></strong><span id="schemeCompetitionActivationText">역세권 입지·구역계 조건을 확인합니다.</span><small id="schemeCompetitionActivationRoute">역세권형 / 간선도로변형 판정 연계</small></button>
+    <button class="scheme-competition-card" type="button" onclick="showCandidateBasis('prior_negotiation')"><strong>사전협상 <em id="schemeCompetitionPriorBadge">추진후보</em></strong><span id="schemeCompetitionPriorText">5,000㎡ 이상과 협상대상지 성격을 확인합니다.</span><small id="schemeCompetitionPriorArea">토지 일괄확보·도시계획변경 필요성 확인</small></button>
+  </div>
+  <div class="scheme-competition-foot" id="schemeCompetitionFoot"><b>판정 원칙</b> · 법적 PASS/FAIL을 변경하지 않고, 두 제도가 경합하는 사업전략 상태만 별도로 표시합니다.</div>
+</section>
 <div class="cc-scheme-icons" id="ccSchemeIcons">
 <div class="cc-scheme-family-label" data-family="housing_renewal">주택정비사업 <small>도정법 · 공통 현황 Fact / 사업별 독립 판정</small></div>
 <button data-family="housing_renewal" data-scheme="redevelopment" onclick="showCandidateBasis('redevelopment')"><i>🏘</i><span>주택재개발</span><em>입력필요</em></button>
@@ -13874,6 +13895,93 @@
   }
   return moduleErrors;
 }
+
+let schemeCompetitionState={active:false,relation_active:false,activation_candidate:false,prior_candidate:false,area_m2:null,parcel_area_m2:null,parcel_adjustment:null,reason:'',activation_routes:null,independent_activation_failures:[],independent_prior_failures:[]};
+function schemeResultRow(name,item){return (schemeResults[name]?.rows||[]).find(r=>r.item===item)||null;}
+function selectedParcelAreaForCompetition(f){
+  const p=f?.properties||{};let a=p.official_area_m2??p._analysis_area_m2??p.geometry_area_m2;
+  if(a==null){try{a=turf.area(turf.feature(f.geometry));}catch(e){a=null;}}
+  const n=Number(a);return Number.isFinite(n)&&n>0?n:null;
+}
+function selectedParcelUnder5000Scenario(){
+  const rows=[];let total=0,known=0;
+  for(const pnu of selectedParcelPnus){
+    const f=parcelFeatureMap.get(String(pnu));if(!f)continue;const a=selectedParcelAreaForCompetition(f);if(a==null)continue;
+    const p=f.properties||{},label=String(p.jibun||p.jibun_nm||p.land_ledger?.mnnmSlno||pnu);rows.push({pnu:String(pnu),label,area_m2:a});total+=a;known++;
+  }
+  if(!(total>=5000)||rows.length<2)return {possible:false,total_m2:total||null,known_count:known,selected_count:selectedParcelPnus.size,options:[]};
+  const options=rows.map(x=>({...x,remaining_m2:total-x.area_m2})).filter(x=>x.remaining_m2>=1500&&x.remaining_m2<5000).sort((a,b)=>b.remaining_m2-a.remaining_m2||a.area_m2-b.area_m2);
+  return {possible:options.length>0,total_m2:total,known_count:known,selected_count:selectedParcelPnus.size,options:options.slice(0,12),best:options[0]||null};
+}
+function analyzeActivationPriorNegotiationCompetition(store){
+  const empty={active:false,relation_active:false,activation_candidate:false,prior_candidate:false,area_m2:null,parcel_area_m2:null,parcel_adjustment:selectedParcelUnder5000Scenario(),reason:'',activation_routes:null,independent_activation_failures:[],independent_prior_failures:[]};
+  if(!store)return empty;
+  const c=store.common||{},area=Number(c.area),activation=schemeResults.activation,prior=schemeResults.prior_negotiation;
+  const parcelAdjustment=selectedParcelUnder5000Scenario();
+  const out={...empty,area_m2:Number.isFinite(area)?area:null,parcel_area_m2:parcelAdjustment.total_m2,parcel_adjustment:parcelAdjustment};
+  if(!activation||!prior||!Number.isFinite(area)||area<5000)return out;
+  const aRows=activation.rows||[],pRows=prior.rows||[];
+  const aLocation=aRows.find(r=>r.item==='입지유형'),aArea=aRows.find(r=>r.item==='대상지 면적'),aPrior=aRows.find(r=>r.item==='사전협상 대상여부');
+  const pArea=pRows.find(r=>r.item==='협상대상지 면적');
+  const unresolved=c.priorNegotiation==null;
+  const activationIndependentFails=aRows.filter(r=>r.required!==false&&r.status==='FAIL'&&r.item!=='사전협상 대상여부');
+  const priorIndependentFails=pRows.filter(r=>r.required!==false&&r.status==='FAIL');
+  const locationAlive=!!aLocation&&aLocation.status!=='FAIL';
+  const activationAreaAlive=!aArea||aArea.status!=='FAIL';
+  const priorAreaAlive=!!pArea&&pArea.status==='PASS';
+  const relationActive=unresolved&&locationAlive&&activationAreaAlive&&priorAreaAlive;
+  const activationCandidate=relationActive&&activationIndependentFails.length===0;
+  const priorCandidate=relationActive&&priorIndependentFails.length===0;
+  const station=activation.facts?.location?.station||{},arterial=activation.facts?.location?.arterial||{};
+  const stationRow=aRows.find(r=>r.item==='역세권'),arterialRow=aRows.find(r=>r.item==='간선도로변');
+  out.relation_active=relationActive;out.activation_candidate=activationCandidate;out.prior_candidate=priorCandidate;out.active=relationActive&&(activationCandidate||priorCandidate);
+  out.independent_activation_failures=activationIndependentFails.map(r=>r.item);out.independent_prior_failures=priorIndependentFails.map(r=>r.item);
+  out.activation_routes={station:{status:stationRow?.status||'REVIEW',value:stationRow?.value||'',note:stationRow?.note||'',distance_m:station.distance_m??null,name:station.name||''},arterial:{status:arterialRow?.status||'REVIEW',value:arterialRow?.value||arterial.note||'',note:arterialRow?.note||'',linear_commercial:arterial.linear_commercial??null,overlap_pct:arterial.site_overlap_pct??null}};
+  const areaText=`${Math.round(area).toLocaleString('ko-KR')}㎡`;
+  out.reason=relationActive?`대상지 ${areaText}로 5,000㎡ 이상이며 역세권활성화 입지경로가 살아 있는 상태에서 사전협상 대상지 여부가 아직 확정되지 않아 두 제도의 적용관계가 경합합니다.`:'';
+  return out;
+}
+function activationRouteUiSummary(){
+  const rows=schemeResults.activation?.rows||[];
+  const station=rows.find(r=>r.item==='역세권'),arterial=rows.find(r=>r.item==='간선도로변');
+  const statusWord=r=>r?.status==='PASS'?'충족':r?.status==='FAIL'?'미충족':'확인필요';
+  const live=[];if(station?.status==='PASS')live.push('역세권형');if(arterial?.status==='PASS')live.push('간선가로형');
+  const short=live.length?live.join('+'):(station?.status==='REVIEW'||arterial?.status==='REVIEW'?'입지확인':'입지미충족');
+  return {short,station_status:station?.status||'REVIEW',arterial_status:arterial?.status||'REVIEW',station_label:`역세권형 ${statusWord(station)}`,arterial_label:`간선가로형 ${statusWord(arterial)}`,linear_commercial:schemeResults.activation?.facts?.location?.arterial?.linear_commercial??null};
+}
+function competitionRouteLabel(x,label){
+  const s=x?.status||'REVIEW',status=s==='PASS'?'충족':s==='FAIL'?'미충족':'확인필요';return `${label} ${status}`;
+}
+function renderSchemeCompetitionPanel(){
+  const el=document.getElementById('schemeCompetitionPanel');if(!el)return;
+  const x=schemeCompetitionState||{};el.classList.toggle('active',x.relation_active===true);
+  if(!x.relation_active)return;
+  const fmtA=n=>Number.isFinite(Number(n))?`${Math.round(Number(n)).toLocaleString('ko-KR')}㎡`:'-';
+  const summary=document.getElementById('schemeCompetitionSummary');if(summary)summary.textContent=`사업판정 구역계 ${fmtA(x.area_m2)} · 5,000㎡ 이상 경계에서 역세권활성화와 사전협상의 적용관계가 경합합니다.`;
+  const ar=x.activation_routes||{},actBadge=document.getElementById('schemeCompetitionActivationBadge'),actText=document.getElementById('schemeCompetitionActivationText'),actRoute=document.getElementById('schemeCompetitionActivationRoute');
+  if(actBadge)actBadge.textContent=x.activation_candidate?'조건부 추진후보':x.independent_activation_failures?.length?'별도 미달조건':'경합검토';
+  if(actText)actText.textContent=x.activation_candidate?'사전협상 대상지로 확정되지 않는 경우 역세권활성화 적용을 계속 검토할 수 있습니다.':'경합관계와 별도로 충족해야 할 역세권활성화 조건이 남아 있습니다.';
+  if(actRoute)actRoute.textContent=`${competitionRouteLabel(ar.station,'역세권형')} · ${competitionRouteLabel(ar.arterial,'간선도로변형')}${ar.arterial?.linear_commercial===true?' · 노선형 상업지역 포함':ar.arterial?.linear_commercial===false?' · 노선형 상업지역 미포함':' · 노선형 상업지역 확인중'}`;
+  const pBadge=document.getElementById('schemeCompetitionPriorBadge'),pText=document.getElementById('schemeCompetitionPriorText'),pArea=document.getElementById('schemeCompetitionPriorArea');
+  if(pBadge)pBadge.textContent=x.prior_candidate?'조건부 추진후보':'별도 확인필요';
+  if(pText)pText.textContent=x.prior_candidate?'5,000㎡ 이상 규모요건을 충족하며 토지 일괄확보·협상대상지 성격·도시계획변경 필요성을 전제로 사전협상 추진을 검토할 수 있습니다.':'사전협상 자체의 독립 필수요건을 추가 확인해야 합니다.';
+  const pa=x.parcel_adjustment||{};if(pArea){
+    if(pa.possible&&pa.best)pArea.textContent=`선택필지 ${fmtA(pa.total_m2)} · 1필지 제외 시 5,000㎡ 미만 조정 가능한 조합 ${pa.options.length}개 (예: ${fmtA(pa.best.area_m2)} 제외 → ${fmtA(pa.best.remaining_m2)})`;
+    else if(Number.isFinite(Number(pa.total_m2)))pArea.textContent=`선택필지 합계 ${fmtA(pa.total_m2)} · 5,000㎡ 미만 조정 시나리오는 구역계 재설정 후 재판정`;
+    else pArea.textContent='선택필지별 면적 확인 후 5,000㎡ 미만 구역계 조정 가능성을 계산합니다.';
+  }
+  const foot=document.getElementById('schemeCompetitionFoot');if(foot){
+    const extra=[];if(x.independent_activation_failures?.length)extra.push(`역세권활성화 별도 미달: ${x.independent_activation_failures.join('·')}`);if(x.independent_prior_failures?.length)extra.push(`사전협상 별도 미달: ${x.independent_prior_failures.join('·')}`);
+    foot.innerHTML=`<b>전략 해석</b> · 법적 PASS/FAIL/REVIEW 원값은 보존합니다. 사전협상 대상지 확정 전에는 경합 자체만으로 역세권활성화를 후보군에서 제거하지 않습니다.${extra.length?' · '+escHtml(extra.join(' / ')):''}`;
+  }
+}
+function schemeCompetitionDisplayOverride(name){
+  const x=schemeCompetitionState||{};if(!x.relation_active||!['activation','prior_negotiation'].includes(name))return null;
+  if(name==='activation'&&x.activation_candidate)return {kind:'adjustable',css:'candidate-adjustable',label:'경합검토',action:'5,000㎡ 이상 경합 · 사전협상 대상지 미확정 시 역세권활성화 계속 검토',competition:true};
+  if(name==='prior_negotiation'&&x.prior_candidate)return {kind:'adjustable',css:'candidate-adjustable',label:'추진후보',action:'5,000㎡ 이상 · 토지 일괄확보 및 협상대상지 성격 충족 전제',competition:true};
+  return null;
+}
+
 function runAllSchemeChecks(){
   const panel=document.querySelector('.scheme-panel');
   if(!panel)return {store:null,moduleErrors:[],uiErrors:[],skipped:true};
@@ -13890,12 +13998,15 @@
 
     // 2) 사업별 검토모듈은 서로 독립 실행한다. 한 모듈 오류는 해당 사업만 자동판정상 FAIL 처리한다.
     moduleErrors.push(...evaluateSchemeModulesSafely(store));
+    // 2-1) 법적 판정 원값을 바꾸지 않고 역세권활성화↔사전협상 경합 전략상태를 별도 계산한다.
+    schemeCompetitionState=analyzeActivationPriorNegotiationCompetition(store);
   }catch(e){
     const issue={scope:'store',name:'fact_store',label:'현황 Fact Store',message:String(e?.message||e)};
     moduleErrors.push(issue);console.error('[scheme-store]',e);
     analysisState.fact_store_error=issue.message;
     latestSiteFactStore=null;latestSchemeModuleResults={};
     analysisState.recommendations=[];analysisState.planning_alternatives=[];
+    schemeCompetitionState={active:false,relation_active:false,activation_candidate:false,prior_candidate:false,area_m2:null,parcel_area_m2:null,parcel_adjustment:null,reason:'',activation_routes:null,independent_activation_failures:[],independent_prior_failures:[]};
     // 전역 Fact Store 오류 시 사업추천·시뮬레이션은 중단한다. 부분자료를 근거로 임의 후보를 만들지 않는다.
     store=null;
   }
@@ -13909,6 +14020,7 @@
     safeSchemeUiStep('factory-usage',()=>renderFactoryUsageSpatialStatus(),uiErrors);
     safeSchemeUiStep('scheme-spatial',()=>renderSchemeSpecificSpatialStatus(store),uiErrors);
   }
+  safeSchemeUiStep('scheme-competition',()=>renderSchemeCompetitionPanel(),uiErrors);
   safeSchemeUiStep('candidate',()=>updateCandidateSchemes(),uiErrors);
   safeSchemeUiStep('semiindustrial-review',()=>renderSemiindustrialDevelopmentPanel(),uiErrors);
   safeSchemeUiStep('ai-analysis',()=>scheduleAiComprehensiveAnalysis(),uiErrors);
@@ -14520,7 +14632,7 @@
 
   if(n)n.textContent=schemeNames[mobileSelectedSchemeName]||mobileSelectedSchemeName;
   if(s){
-    s.textContent=display.kind==='available'?'현재 추진 가능':display.kind==='alternative'?'현재 추진 가능 · 대안':display.kind==='adjustable'?'조건변경 후 가능':display.kind==='pending'?'판정 유보':'적용 곤란';
+    s.textContent=display.competition?'제도경합 · 조건부 추진후보':display.kind==='available'?'현재 추진 가능':display.kind==='alternative'?'현재 추진 가능 · 대안':display.kind==='adjustable'?'조건변경 후 가능':display.kind==='pending'?'판정 유보':'적용 곤란';
     s.className=['available','alternative'].includes(display.kind)?'PASS':display.kind==='adjustable'?'FAIL':'REVIEW';
     s.title=display.action||'';
   }
@@ -14893,6 +15005,7 @@
 function candidateDisplayState(name,st=safeCandidateState(name)){
   if(!st?.ready)return {kind:'pending',css:'candidate-review',label:'분석대기',action:'구역계·현황자료 입력 필요'};
   if(SHELL_SCHEMES.has(name))return {kind:'pending',css:'candidate-review',label:'추후보완예정',action:'자동판정 모듈 미구축'};
+  const competitionOverride=schemeCompetitionDisplayOverride(name);if(competitionOverride)return competitionOverride;
   if(st.state==='on'){
     if(name==='urban_redevelopment'){
       const ps=(schemeResults.urban_redevelopment||latestSchemeModuleResults.urban_redevelopment)?.facts?.path_summary||{};
@@ -15070,7 +15183,7 @@
     const fail=st.failReasons?.length?`<div>조건미달: ${escHtml(st.failReasons.join(' · '))}</div>`:'';
     const review=st.reviewReasons?.length?`<div>추가확인: ${escHtml(st.reviewReasons.join(' · '))}</div>`:'';
     return `<article class="priority-rank-card">
-      <div><span class="priority-rank-no">${i+1}순위</span><span class="priority-rank-status">${escHtml(display.kind==='available'?'현재 추진 가능':display.kind==='alternative'?'현재 추진 가능 · 대안':'조건변경 후 가능')}</span></div>
+      <div><span class="priority-rank-no">${i+1}순위</span><span class="priority-rank-status">${escHtml(display.competition?'제도경합 · 조건부 추진후보':display.kind==='available'?'현재 추진 가능':display.kind==='alternative'?'현재 추진 가능 · 대안':'조건변경 후 가능')}</span></div>
       <div class="priority-rank-name">${escHtml(schemeNames[name]||name)} <small>· ${escHtml(schemeFamilyLabel(name))}</small></div>
       <div class="priority-rank-reason">${escHtml(rankingReason(name))}${fail}${review}</div>
       ${display.kind==='adjustable'?`<div class="priority-rank-density"><b>변경조건</b> · ${escHtml(display.action)}</div>`:''}
@@ -15202,7 +15315,7 @@
   const display=candidateDisplayState(name,st);
   const cnt=candidateRegCounts(name);
   const bits=[
-    schemeFamilyLabel(name),display.kind==='available'?'현재 추진 가능':display.kind==='alternative'?'현재 추진 가능(대안추천)':display.kind==='adjustable'?`조건변경 후 가능(${display.action})`:display.kind==='pending'?'판정 유보':'적용 곤란',
+    schemeFamilyLabel(name),display.competition?`사업성 제고제도 경합(${display.action})`:display.kind==='available'?'현재 추진 가능':display.kind==='alternative'?'현재 추진 가능(대안추천)':display.kind==='adjustable'?`조건변경 후 가능(${display.action})`:display.kind==='pending'?'판정 유보':'적용 곤란',
     st.purposeGate==='priority'?'목적 직접적합':st.purposeGate==='secondary'?'목적 대안':'목적 하드제외',
     st.structural==='PASS'?'법정진입 확인':st.structural==='REVIEW'?'법정진입 확인필요':'법정진입 미달',
     st.regulatory==='PASS'?'정량요건 충족':st.regulatory==='REVIEW'?`정량 ${cnt.review}건 확인`:'정량요건 미달'
@@ -15267,7 +15380,7 @@
       if(legal.length>=3)break;
     }
     return {
-      scheme:name,business:schemeNames[name],family:schemeFamilyLabel(name),status:aiDecisionStatus(name,res,st,display),engine_status:res.overall||'REVIEW',display_label:aiCompactText(display.label,100),condition_change:display.kind==='adjustable'?aiCompactText(display.action,140):'',
+      scheme:name,business:schemeNames[name],family:schemeFamilyLabel(name),status:aiDecisionStatus(name,res,st,display),engine_status:res.overall||'REVIEW',display_label:aiCompactText(display.label,100),condition_change:display.kind==='adjustable'?aiCompactText(display.action,140):'',strategic_competition:display.competition===true,
       strengths:(res.strengths||[]).slice(0,3).map(x=>aiCompactText(x,120)),risks:(res.risks||[]).slice(0,3).map(x=>aiCompactText(x,120)),followups:(res.followups||[]).slice(0,3).map(x=>aiCompactText(x,120)),gaps,planning_required,prerequisites,
       far:aiCompactText(den.far||'',120),zoning_up:aiCompactText(den.zone||'',100),public_contribution:aiCompactText(den.contribution||'',120),legal_basis:legal
     };
@@ -15305,6 +15418,7 @@
       overlaps:{renewal:(site.renewal?.overlaps||[]).slice(0,6).map(x=>({name:x.name,type:x.type,overlap_pct:x.overlap_pct})),district_plans:(site.development?.district_plans||[]).slice(0,6).map(x=>({name:x.name,area_m2:x.area_m2})),planning_facilities:(site.development?.planning_facilities||[]).slice(0,8).map(x=>({name:x.name,category:x.category,area_m2:x.area_m2}))}
     },
     regulatory_constraints:{policy:'기존 사업판정과 독립된 추가검토 FACT · AI가 PASS/FAIL 변경 금지',confirmed:regulatoryConfirmed,unknown:regulatoryUnknown,errors:(regulatory.errors||[]).slice(0,8)},
+    scheme_competition:schemeCompetitionState?.relation_active?{type:'ACTIVATION_PRIOR_NEGOTIATION',area_m2:schemeCompetitionState.area_m2,activation_candidate:schemeCompetitionState.activation_candidate,prior_candidate:schemeCompetitionState.prior_candidate,reason:schemeCompetitionState.reason,parcel_adjustment:schemeCompetitionState.parcel_adjustment,activation_routes:schemeCompetitionState.activation_routes}:null,
     business_results:businessResults,
     recommended_business:recInfo(top[0]),alternative_businesses:top.slice(1,3).map(recInfo).filter(Boolean),unknown_items:unknown,
     analysis_policy:{role:'existing_engine_explanation_only',unknown_rule:'REVIEW/UNKNOWN은 충족·미충족으로 단정하지 않음',legal_rule:'입력에 없는 법적 기준·수치 생성 금지',regulation_rule:'입력 FACT에 없는 규제를 추정하지 않고 관계기관 협의·최종 허용여부를 단정하지 않음',design_caveat:'대지형상·문화재·높이·경관·건축배치 등은 별도 전문검토'}
@@ -16105,9 +16219,9 @@
         btn.classList.add(`density-tier-${density.tier}`);
         btn.dataset.densityTier=String(density.tier);btn.dataset.densityLabel=densityTierLabel(density.tier);
         btn.dataset.displayState=display.kind;
-        const em=btn.querySelector('em');if(em)em.textContent=display.label;
+        const em=btn.querySelector('em');if(em){const routeMeta=name==='activation'?activationRouteUiSummary():null;em.textContent=name==='activation'?`${display.label} · ${routeMeta.short}`:display.label;}
         btn.dataset.engineStage=st.stage||'';
-        const reasonTip=[`${display.kind==='available'?'현재 추진 가능':display.kind==='alternative'?'현재 추진 가능 · 대안':display.kind==='adjustable'?'조건변경 후 가능':display.kind==='pending'?'판정 유보':'적용 곤란'} · ${display.action}`,`${densityTierLabel(density.tier)} · ${density.reason}`,st.failureDetail?`미달: ${st.failureDetail}`:'',st.reviewDetail?`확인: ${st.reviewDetail}`:''].filter(Boolean).join('\n');
+        const reasonTip=[`${display.competition?'사업성 제고제도 경합':display.kind==='available'?'현재 추진 가능':display.kind==='alternative'?'현재 추진 가능 · 대안':display.kind==='adjustable'?'조건변경 후 가능':display.kind==='pending'?'판정 유보':'적용 곤란'} · ${display.action}`,`${densityTierLabel(density.tier)} · ${density.reason}`,st.failureDetail?`미달: ${st.failureDetail}`:'',st.reviewDetail?`확인: ${st.reviewDetail}`:''].filter(Boolean).join('\n');
         btn.title=reasonTip;
         ranked.push({name,rank:st.rank||0,state:st.state,label:display.label,display,density});
       }else{
@@ -16129,7 +16243,7 @@
         purpose_gate:st.purposeGate,structural_gate:st.structural,area_gate:st.areaGate,area_reason:st.areaReason||'',regulatory:st.regulatory,execution:st.execution,
         relations:st.relations||[],fail_reasons:st.failReasons||[],review_reasons:st.reviewReasons||[],failure_detail:st.failureDetail||'',review_detail:st.reviewDetail||'',
         strengths:schemeResults[x.name]?.strengths||[],risks:schemeResults[x.name]?.risks||[],followups:schemeResults[x.name]?.followups||[],module:schemeResults[x.name]?.module||'shell',
-        density_potential:x.density||safeDensityPotentialForScheme(x.name,st),reasons:st.reasons||[]
+        density_potential:x.density||safeDensityPotentialForScheme(x.name,st),strategic_competition:(['activation','prior_negotiation'].includes(x.name)&&schemeCompetitionState?.relation_active)?{active:true,candidate:x.name==='activation'?schemeCompetitionState.activation_candidate:schemeCompetitionState.prior_candidate,area_m2:schemeCompetitionState.area_m2,parcel_adjustment:schemeCompetitionState.parcel_adjustment}:null,activation_routes:x.name==='activation'?activationRouteUiSummary():null,reasons:st.reasons||[]
       };
     }catch(e){console.error(`[candidate-decision:${x.name}]`,e);}
   }
@@ -16144,7 +16258,7 @@
       if(!activeGeometry)sum.textContent='지도에 그리기·지번 입력·SHP 중 하나로 구역계를 설정하세요.';
       else if(analysisState.fact_store_error)sum.textContent='현황 Fact Store 오류로 사업추천을 중단했습니다. 오류 항목을 복구한 뒤 다시 검토하세요.';
       else if((latestSiteFactStore?.common||commonSchemeData()).area==null)sum.textContent='구역계 설정 완료 · ‘검토하기’를 실행하면 공식 필지면적과 공간정보를 수집해 사업방식을 판정합니다.';
-      else {const altCount=ranked.filter(x=>x.display?.kind==='alternative').length;const primaryAvailable=Math.max(0,available-altCount);sum.textContent=available+adjustable?`현재 추진 가능 ${primaryAvailable}개 · 대안추천 ${altCount}개 · 조건변경 후 가능 ${adjustable}개 · 판정 유보 ${pending}개 · 적용 곤란 ${unavailable}개`:'현재 조건 또는 계산 가능한 조건변경으로 추진 가능한 사업방식이 없습니다.';}
+      else {const altCount=ranked.filter(x=>x.display?.kind==='alternative').length;const primaryAvailable=Math.max(0,available-altCount);const competitionCount=ranked.filter(x=>x.display?.competition===true).length;sum.textContent=available+adjustable?`현재 추진 가능 ${primaryAvailable}개 · 대안추천 ${altCount}개 · 조건변경 후 가능 ${adjustable}개${competitionCount?`(제도경합 ${competitionCount}개)`:''} · 판정 유보 ${pending}개 · 적용 곤란 ${unavailable}개`:'현재 조건 또는 계산 가능한 조건변경으로 추진 가능한 사업방식이 없습니다.';}
     }
   }catch(e){console.error('[candidate-summary]',e);}
 
@@ -17539,7 +17653,7 @@
   let reason='-';try{reason=rankingReason(name)||'-';}catch(e){reason=st.reviewDetail||'일부 판정오류로 근거 확인 필요';}
   const area=store?.site?.area_m2??null;
   root.innerHTML=`<div><div class="priority-preview-rank"><span>1순위</span><strong>${escHtml(schemeNames[name]||name)}</strong></div><table class="priority-preview-table"><tbody>
-    <tr><th>판정</th><td>${escHtml(st.label||activationFeasibilityLabel(res.overall||'REVIEW'))}</td></tr>
+    <tr><th>판정</th><td>${escHtml(candidateDisplayState(name,st).competition?'제도경합 · '+candidateDisplayState(name,st).label:(candidateDisplayState(name,st).label||st.label||activationFeasibilityLabel(res.overall||'REVIEW')))}</td></tr>
     <tr><th>대상면적</th><td>${escHtml(fmtSchemeArea(area))}</td></tr>
     <tr><th>현황 용도지역</th><td>${escHtml(c.zoning||'-')}</td></tr>
     <tr><th>검토 용도지역</th><td>${escHtml(target)}</td></tr>
