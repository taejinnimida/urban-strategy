# R29 검증보고서 — 서울플랜+ 기존 추진사업 통합현황 1차

기준본: `urban-strategy-v2.5.0-20260911-streetblock-browser-unfreeze-r28-PATCH.zip`

## 1. 수정 목적

1. 기존 정비구역/재정비촉진 중심의 공간현황을 서울플랜+ UQ120 전체 사업현황으로 확장한다.
2. 역세권 장기전세주택, 역세권활성화, 청년안심주택, 미리내집, 도심 공공주택 복합사업 등 기존 추진사업을 대상지 중첩 FACT로 직접 식별한다.
3. 사업명과 현재 추진단계를 지도 툴팁/범례에 표시한다.
4. UQ120의 `CREATE_DAT`를 고시일로 오인하지 않고 데이터 생성일로 분리한다.
5. 기존 법정 정비구역 판정용 `renewalAnalysis.overlaps`와 기존 추진사업 레지스트리를 분리하여 사업별 Rule 결과를 오염시키지 않는다.

## 2. 공식 UQ120 코드표 감사

`uq120_project.zip` 내부 서울플랜+ 코드정의표 기준으로 확인한 사업유형은 다음 26종이다.

- 정비사업: BZ101, BZ102, BZ103, BZ104, BZ105, BZ107, BZ108
- 소규모 정비사업: BZ201~BZ205
- 역세권사업: BZ301 장기전세주택, BZ302 역세권 활성화, BZ303 청년안심주택, BZ306 미리내집
- 재정비촉진사업: BZ401~BZ404
- 국토부사업: BZ501 공공주택지구조성사업, BZ502 도심 공공주택 복합사업
- 기타사업: BZ601 도시개발사업, BZ602 리모델링활성화구역, BZ603 시장정비사업, BZ604 도시재생활성화지역

실제 2026-02 UQ120 SHP 총 2,585건을 전부 읽었으며 주요 건수는:

- BZ301 장기전세주택 54건
- BZ302 역세권 활성화 62건
- BZ303 청년안심주택 137건
- BZ306 미리내집 58건
- BZ401 재정비촉진지구 31건
- BZ402 재정비촉진구역 213건
- BZ502 도심 공공주택 복합사업 34건

## 3. 추진단계 코드

UQ120 코드정의표의 `PROPEL_CD`를 `project_stage_label`로 변환한다.
예:

- PP0806 = 구역지정
- PP0807 = 건축심의
- PP0808 = 사업계획승인
- PP1806 = 구역지정
- PP1807 = 건축심의/통합심의
- PP1808 = 사업계획승인
- PP1203 = 지구지정
- PP1205 = 사업계획승인

현재 SHP의 `PROPEL_CD`는 현재 추진단계 FACT이며, 과거 단계별 날짜 이력은 포함하지 않는다.

## 4. R29 구현

### 4.1 백엔드

- `PLANPLUS_PROJECT_TYPES` 추가
- `PLANPLUS_STAGE_LABELS` 추가
- `_planplus_project_reference_data()` 추가
- `_planplus_project_spatial_index()` 추가
- `_planplus_project_intersections()` 추가
- `/api/spatial/renewal-intersections` 응답에 아래 필드 추가
  - `project_registry_overlaps`
  - `project_registry_context_features`
  - `project_registry_metadata`

기존 정비법/사업별 판정이 참조하는 `overlaps`는 그대로 유지한다.
따라서 역세권 장기전세/활성화 등 UQ120 특례사업이 기존 정비구역 배제조건에 잘못 섞이지 않는다.

### 4.2 데이터 의미 보정

기존 코드가 UQ120/UQ181의 `CREATE_DAT`를 `notice_date`로 저장하던 부분을 제거했다.

- `notice_date`: 실제 결정고시 이력 연계 전까지 빈 값
- `data_reference_date`: `CREATE_DAT`

즉 데이터 생성일을 고시일/사업승인일로 표시하지 않는다.

### 4.3 UI

정비구역 현황 카드에서:

- `예정·후보·사업구역` → `서울플랜+ 추진사업`
- `기존 고시·추진사업` 목록 추가
- 대상지 중첩 사업별 표시:
  - 사업유형
  - 사업명/구역명
  - 현재 추진단계
  - 대상지 중첩률
- 지도 툴팁에도 사업유형/사업명/현재단계 표시
- 법정 UQ181과 UQ120 사업현황을 동일 미니맵에서 중복 없이 표시

## 5. 런타임 교차검증

공식 UQ120/UQ181 원자료를 로컬에 연결해 직접 실행했다.

- UQ120 전체 2,585건 로딩 성공
- BZ301 장기전세주택 54건 인식
- BZ302 역세권활성화 62건 인식
- BZ303 청년안심주택 137건 인식
- BZ502 도심 공공주택 복합사업 34건 인식

BZ301 장기전세주택 도형 자체를 테스트 구역계로 사용했을 때:

- `역세권 장기전세주택 / 불광동 227-7 / 건축심의 / 100% 중첩`
- 동일 위치의 `재개발(도시정비형) / 독바위역세권 / 건축심의 / 100% 중첩`

을 각각 독립 사업 FACT로 동시에 반환하는 것을 확인했다.

## 6. 아직 남은 이력정보

R29는 **사업명 + 현재 추진단계**까지의 1차 업그레이드다.
사용자가 요구한 다음 형태:

`상봉1구역 : 구역지정 25.xx.xx / 정비계획수립 26.02.22`

처럼 단계별 실제 날짜를 만들려면 서울도시공간포털 결정고시, 정비사업 정보몽땅, 서울시/자치구 고시·공고의 이력 결합이 추가로 필요하다.

현재 단계에서 날짜가 없는 항목을 임의 추정하거나 `CREATE_DAT`로 대체하지 않는다.

## 7. 회귀검사

- `python -m py_compile app.py`: PASS
- Inline JavaScript `node --check`: PASS
- R15: PASS
- R17: 15/15 PASS
- R18: 15/15 PASS
- R19: 8/8 PASS
- R20: 10/10 PASS
- R21: 16/16 PASS
- R22: 13/13 PASS
- R23: 26/26 PASS
- R24: 18/18 PASS
- R25: 15/15 PASS
- R26: 37/37 PASS
- R27: 25/25 PASS
- R28: 14/14 PASS
- R29 신규: 27/27 PASS

## 8. 보존사항

- R20 면적 선필터 유지
- R21 접도율 4m/6m 유지
- R22 prepared geometry 유지
- R23 lazy neighbor graph 유지
- R24 지번 입력/지도 클릭 유지
- R25 역세권활성화 세부판정 유지
- R26 접도 Fact 사업별 연결 및 면적 10% 추천필터 유지
- R27 대안추천/도로 FACT 정합화 유지
- R28 브라우저 가로구역 후처리 경량화 유지
- 내부 앱 버전 `v2.5.0` 유지
