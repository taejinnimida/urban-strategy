# GPT PATCH NOTES — 사업구역·가로구역 추출 검증본

기준: `urban-strategy-v2.5.0-r31-boundary-frontage-deferred.zip`
앱 내부 버전: `2.5.0` 유지

## 목적
현재 공간자료로 `project_area`와 복수 `street_blocks[]`를 실제로 생성할 수 있는지 먼저 검증한다. 이번 빌드에서는 기존 사업 Rule 결과에 연결하지 않는다.

## 1. 사업구역 project_area — 플랫폼 조작적 정의
- 사용자 검토요청지를 상한 경계로 사용한다.
- 외곽 경계는 도로를 최우선으로 사용한다.
- 검토요청지 단부의 지목상 도로·철도용지는 사업구역에서 제척한다.
- 도시계획시설 도로·철도·하천이 검토요청지 단부와 만나는 경우 사업구역에서 제척한다.
- 공원·녹지·광장·공공공지·공용주차장·학교는 검토요청지 단부에 있으면서 공식 시설도형 면적이 1,500㎡를 초과할 때 제척한다.
- 위 시설이 내부에 있으면 포함한다.
- 단부 시설이라도 1,500㎡ 이하이면 포함한다.
- 내부 지목상 도로는 사업구역을 나누지 않도록 폐합한다.
- 필지군이 끊겨 MultiPolygon이 되는 경우 검토요청지 안에서 직선 폐합 후보를 만들고 외곽 제척시설을 다시 차감한다.
- 출력: 붉은 2점쇄선 `사업구역`, 면적 자동 산정.

## 2. 가로구역 street_blocks[] — 플랫폼 조작적 정의
사업구역 내부에서 다음 시설을 분할 경계로 사용한다.
- 도로
- 공용주차장
- 광장
- 공원
- 녹지
- 공공공지
- 하천
- 철도
- 학교

도로는 4m 미만이면 폐합한다.
- 지목상 도로 및 도시계획시설 도로 polygon의 짧은 폭을 도형으로 임시 추정한다.
- 추정폭 4m 이상: 가로구역 separator 사용.
- 4m 미만: separator에서 제외하여 폐합.
- 폭 추정 불가: 전문가 확인 대상으로 기록. 도시계획시설 도로는 검증용 범위 추출을 위해 separator 후보로 사용하되 REVIEW 표시.

분할된 polygon을 면적순으로 `가구역`, `나구역`, `다구역` … 으로 명명하고 각각:
- 면적
- 사업구역 대비 비율
- 추출근거
- 전문가 확인 상태
를 표로 표시한다.

## 3. UI
기존 `가로구역 분석범위·대상지 공간관계` 패널을 검증용 `사업구역·가로구역 검토`로 교체.
- 검은선: 사용자 검토요청지
- 붉은 2점쇄선: 사업구역
- 주황면: 가로구역 후보
- 파랑면: 분할에 사용한 도로·시설
- 가로구역별 면적 테이블 추가

## 4. 향후 Rule 연결 원칙(이번 빌드 미연결)
- 모든 사업 Rule의 공간기준은 `project_area`로 전환 예정.
- `street_blocks[]`는 역세권 등 제도별 범위/면적 판정에 사용 예정.
- 가로구역 정의의 법적 적합성은 전문가 확인으로 유보.
- 자동 가로구역이 제도별 면적·포함범위 정량기준을 충족하는 경우 초기검토는 긍정적으로 충족 처리하고 전문가 확인 단서를 부여하는 방향.

## 5. 검증
- `python -m py_compile app.py` PASS
- `node --check` 인라인 JS PASS
- 수정 관련 회귀검사 `check_r14_street_block_auto`, `check_r15_street_block_4m_conditional` PASS
- `check_spatial_evidence_maps`, `check_startup_drawing_and_legacy_ui` PASS
- 전체 회귀검사는 기존 기준본 자체에 없는 공식 PDF를 요구하는 `r10 scheme fail-safe` 직전까지 모두 PASS. 이번 패치 오류가 아니라 기존 릴리스 파일 검사 중단.

## 주의
이번 버전은 **범위 생성 가능성 검증본**이다. 사업구역·가로구역이 화면에서 의도한 형상으로 생성되는지 실제 사례지에서 확인한 뒤, 면적·역세권 Rule의 authoritative Fact로 승격한다.
