# R19 GitHub 기준 보유자료 우선 분석·연결 오류 수정

## 기준본과 적용
- 기준: 사용자가 지정한 GitHub 실행 코드, main 커밋 7146587ba27ba32ce510d1347ab2357496dd4f6a.
- R18 전체를 덮어쓰지 않았습니다. 누락된 경로와 직접 필요한 자료 처리 함수만 참고·검증하여 복구했습니다.
- 앱 내부 버전 v2.5.0 유지. R19는 이번 교체 패키지 식별자입니다.
- 이 ZIP은 전체 저장소가 아닌 교체용 패키지입니다.
- 저장소 루트의 app.py와 app.html 두 파일을 함께 교체하고 같은 배포로 반영하세요.
- 기존 data 폴더와 모든 원자료·Dockerfile은 유지하세요.
- rollback 폴더는 수정 전 GitHub의 두 원본입니다. 복구 시 이 두 파일을 저장소 루트에 되돌리세요.
- GitHub 원격 쓰기 및 Render 배포는 수행하지 않았습니다.

## 확인한 원인과 수정
1. 화면에서 호출하던 규제 원도형·산업단지·ECVAM 경로가 GitHub app.py에 없었습니다. 호출에 대응하는 경로와 직접 의존 함수를 복구했습니다.
2. 로컬 개발사업 분기가 빈 결과를 즉시 반환하고, 로컬 재해 분기도 조회를 생략했습니다. 보유 UQ181·자연재해위험개선지구·산사태 자료 분석을 연결했습니다.
3. 가로구역 외부 조회가 보유자료보다 먼저 시작됐습니다. 보유자료를 순차 완료한 후 외부 자료 수집, 도로 FACT, 가로구역 분석으로 진행합니다.
4. 보전환경 localOnly 경로는 원자료 실패 시에도 외부 NED로 넘어가지 않습니다.
5. 외부 규제 보강의 실패가 이미 확인된 동일 대상지 로컬 결과를 덮지 않도록 보존하며, 규제 분석의 이전 실행·이전 구역 응답은 반영하지 않습니다.
6. 가로구역 분석 범위에 사용 가능한 도로폭이 하나도 없으면 시설조회·가로구역 계산을 보류하고 REVIEW를 유지합니다. 일부 도로폭이 있다는 사실만으로 완전성을 확정하지 않습니다.
7. VWorld 공통 전송부에 동시 호출 제한, 요청별 연결·읽기 대기 제한, 직접·대체 경로 실패 누적 시 30초 대기, 안전한 오류 기록을 적용했습니다. 오류 응답을 데이터로 사용하지 않습니다.
8. 용도지역 단일 조회 진단: 서비스 주소 뒤에 /api/vworld/test?layer=LT_C_UQ111&lon=126.978&lat=37.566 를 붙여 확인합니다. 좌표는 실제 대상지로 바꿀 수 있습니다. 키 값을 URL에 넣지 않습니다. 응답의 transport에서 직접/대체 경로 상태·소요시간·실패 유형을 확인할 수 있습니다.
9. SHP 속성 누락이나 후보 레코드 처리 실패를 정상 비중첩으로 표시하지 않도록 오류로 분리했습니다.

## 먼저 실행하는 보유자료
정비사업 UQ120/UQ181, 개발사업 UQ181, 보유 규제 13종, 자연재해위험개선지구·산사태, 지형, 안심주택 도심배제, 학교 절대보호구역, 비오톱·공익용산지·개발제한구역.
의료시설 대표필지 등 외부 조회가 혼합된 기능은 후속 단계로 유지합니다.

## 검증 결과
- Python 테스트 12개 통과: 외부 통신 금지 상태에서 로컬 API 9개 응답 및 규제 13종 known 확인, 산업단지 경로 연결, 내부/외부/경계 공간 중첩, 필수 속성 누락, 직접 성공, 대체 경로 복구, 실패 누적·회복, HTML 오류 응답 거부, 404 재호출 방지, 키 미노출 진단.
- JavaScript 동작 테스트 3개 통과: 보유자료 완료 전 외부조회 금지, 로컬 단일 실패 후 계속 진행, 도로폭 미확보 시 시설조회·가로구역 계산 보류.
- Python 컴파일, 전체 inline JavaScript 구문 검사, git diff 공백 검사 통과.
- 기존 서버 함수 삭제 없음. 기존 서버 함수 변경은 _vworld_get, analyze_development_intersections, vworld_test, _analyze_local_polygon_zip 네 개입니다. 그 외 추가 경로·함수입니다.
- UI 구성과 사업별 법적 수치·판정식을 변경하지 않았습니다.

## 검증 한계와 배포 후 확인
- 502의 운영 환경 원인과 실제 VWorld 연결 회복은 미확인입니다. 통신 모의시험은 장애 억제·복구 처리 검증이며 실제 외부망 정상화 증거가 아닙니다.
- ECVAM 실제 키 인증 및 지도 응답, 실제 브라우저의 전체 분석·팝업 도면은 배포 후 확인해야 합니다.
- 원자료 전체의 모든 지역·경계 정확성을 검증한 것은 아닙니다. 공간 계산은 합성 내부/외부/경계와 보유자료 표본 API로 확인했습니다.
- 먼저 이전 오류 대상지를 실행해 로컬 결과가 먼저 표시되는지, 세 경로 404가 없어졌는지 확인하세요. 이후 용도지역 단일 진단 결과로 502 실패 구간을 확인하세요.

## 재현 명령
PYTHONPATH=. python -m pytest -q tests/test_pipeline_repair.py
node tests/pipeline_order.test.cjs
