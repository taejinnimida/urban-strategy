# R36 검증보고서 — 접도분석 경계구간 시각화

기준본: `urban-strategy-v2.5.0-20260914-route-commercial-model-reference-r35-PATCH.zip`

## 1. 수정 목적

오류역 인근 대상지처럼 실제로는 복수 도로에 둘러싸인 것으로 보이는데 자동 접도엔진이 `4m+ 1면 / 8m+ 1면`으로 산정하는 경우, 사용자가 **어느 대상지 경계구간을 접도로 인정했고 어느 구간을 놓쳤는지 직접 검증**할 수 있도록 접도현황 미니맵을 진단도면으로 보강했다.

이번 R36은 **시각화/진단 전용 패치**다. 접도 판정식과 사업별 Rule은 수정하지 않았다.

## 2. 추가된 도면요소

- 파랑 굵은 선: `ROAD_BT >= 8m`로 현재 엔진이 인정한 대상지 접도 경계구간
- 초록 굵은 선: `4m <= ROAD_BT < 8m` 인정 접도 경계구간
- 빨강 굵은 선: `ROAD_BT < 4m` 접도 경계구간
- 주황 점선: 연속지적/토지대장상 `지목=도로` 필지와 대상지 경계가 맞닿은 접면 후보
- 옅은 주황 폴리곤: 지목상 도로필지
- 회색 점선: `TL_SPRD_MANAGE` 도로중심선
- 기존 도로폭 버퍼 도형 및 대상지 경계 유지

주황 레이어는 **진단용**이며 현재 PASS/FAIL 입력에 사용하지 않는다. 따라서 `지목 도로 접면후보는 있는데 ROAD_BT 인정 접도선이 없는 구간`을 현행 중심선 버퍼 방식의 불일치 후보로 시각적으로 찾을 수 있다.

## 3. 보존된 기존 판정 로직

R35 대비 다음 함수 블록은 바이트 기준 동일함.

- `siteRoadNetworkStats()` SHA-256 앞 16자리: `df46b4527b306f27`
- `schemeFrontageEvidenceFacts()` SHA-256 앞 16자리: `194c0ce2f5781dc9`
- `schemeRoadEvidenceFacts()` SHA-256 앞 16자리: `4c83fe2c511ccd4e`
- `analyzeRoadAccess()` SHA-256 앞 16자리: `e3b9de1c3371e2a1`

따라서 다음 기준은 그대로 유지된다.

- 역세권활성화: `4m 이상 접도 2면 + 최소 1면 8m 이상`
- 접도면수: 해당 폭원 조건의 접촉길이 `0.5m 이상`인 독립 도로그룹 수
- 접도기하: 현행 `TL_SPRD_MANAGE + ROAD_BT/2 중심선 버퍼`
- R35 노선형 상업지역 MODEL_REFERENCE 로직

## 4. 신규 함수

- `frontageBoundarySegments()`
  - 기존 `frontageStats()`와 동일한 0.5m 샘플 간격과 0.45m 허용범위를 이용해 실제 대상지 외곽선의 접촉구간을 LineString으로 복원
  - 판정값을 새로 계산하거나 덮어쓰지 않고 도면 표시용 Feature만 생성
- `roadConditionCadastralRoadFeatures()`
  - 현재 확보된 연속지적 중 `지목=도로`만 수집
- `roadConditionFrontageVisualFacts()`
  - ROAD_BT 인정선과 지목 도로 접면후보를 분리 반환

## 5. 검증

- R36 신규 회귀검사: **24/24 PASS**
- R35 노선상업 회귀검사: **33/33 PASS**
- Inline JavaScript `node --check`: **PASS**
- `app.py` Python compile: **PASS**

## 6. 오류역 재검토 방법

배포 후 `서울특별시 구로구 경인로 158 일대`를 동일 구역계로 다시 분석하고 `사이트 분석 > 접도현황` 도면에서 다음을 확인한다.

1. 북측 경인로가 파랑(8m+) 인정선으로 표시되는지
2. 동측 오류로/오류지하차도 측 경계에 주황 `지목 도로 접면후보`가 표시되는지
3. 2번은 표시되지만 파랑/초록 인정선이 없으면 현행 중심선 버퍼 접도기하의 false negative 후보로 확정
4. 그 결과를 근거로 다음 패치에서 `지적 도로 접면 → ROAD_BT 폭원 연결` 방식으로 접도엔진 구조 개선 여부 결정

## 7. 비수정 범위

- 접도 판정 PASS/FAIL 식
- ROAD_BT 폭원 산정식
- 역세권활성화 사업 Rule
- 가로구역/사업구역
- R35 노선상업 참조도형 및 판정
- 구릉지 모듈
- 기타 사업모듈 및 UI

내부 앱 버전은 `v2.5.0`을 유지한다.
