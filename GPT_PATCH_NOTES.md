# GPT PATCH NOTES — 사업구역·가로구역 추출 검증본 v2

기준: `urban-strategy-v2.5.0-r31-project-streetblock-validation.zip`
앱 내부 버전: `2.5.0` 유지

## 수정 목적
1차 검증본에서 **검토요청지 경계에 닿는 내부 관통도로를 외곽도로로 오인**하고, 그 결과 사업구역이 MultiPolygon이 되었을 때 **큰 한 조각만 남기는 문제**가 확인됐다.

이번 수정의 완료 기준은 다음 사례다.
- 내부를 관통하는 4m 이상 도로 1개
- `project_area`는 내부도로를 포함한 하나의 사업구역
- `street_blocks[]`는 그 도로를 분할선으로 복원하여 2개(`가구역`, `나구역`) 생성

## 1. 사업구역 project_area 수정

### 외곽도로 판정 방식 변경
기존: 검토요청지 외곽에 조금이라도 닿으면 외곽도로로 제척.

수정: 도로도형과 검토요청지의 관계를 `projectBoundaryRelation()`으로 계산한다.
- 검토요청지 경계부 6m 띠와 도로도형의 중첩면적 비율을 계산
- 도로도형의 **55% 이상이 경계부 띠에 놓이는 경우만 외곽도로**로 분류
- 단순히 입구/출구가 검토요청지 경계에 닿는 관통도로는 내부도로로 유지

즉, **경계를 따라가는 도로는 제척 / 내부로 깊게 관통하는 도로는 사업구역에 포함**한다.

### MultiPolygon 큰 조각 선택 제거
- `polygonOuterShellOnly(merged)` 방식으로 가장 큰 1개 polygon만 남기지 않는다.
- `fillPolygonHolesPreserveParts()`를 사용해 내부 hole은 메우되 다른 토지군을 버리지 않는다.
- 그래도 여러 조각이 남으면 검토요청지 내부에서 볼록외피 방식의 임의 직선 폐합을 허용한다.
- 외곽 제척시설은 폐합 뒤 다시 차감한다.

### 공공시설 기준 유지
- 도로: 외곽경계 최우선
- 철도·하천: 외곽에서 제척
- 공원·녹지·광장·공공공지·공용주차장·학교: 검토요청지 단부 + 시설 전체 1,500㎡ 초과일 때만 제척
- 내부시설 또는 1,500㎡ 이하 단부시설: 사업구역 포함

## 2. 가로구역 street_blocks[] 수정
- 사업구역 생성이 끝난 뒤 **별도 단계**로 분할한다.
- 지목상 내부도로는 사업구역에서는 포함하지만, 가로구역에서는 폭 4m 이상이면 separator로 복원한다.
- 도시계획시설 내부도로도 동일하게 4m 이상이면 separator로 사용한다.
- 공용주차장·광장·공원·녹지·공공공지·하천·철도·학교는 가로구역 분할시설로 사용한다.
- 4m 미만 도로는 폐합한다.
- 폭원 미확정 도로는 전문가 확인 대상으로 남긴다.
- 결과를 `가구역`, `나구역`, `다구역`…으로 명명하고 면적을 개별 산정한다.

## 3. 면적 관계 표시 보완
가로구역 합계가 사업구역 100%와 반드시 같지는 않다.

`가구역 + 나구역 + ... + 분할시설 면적 ≒ 사업구역 면적`

따라서 패널의 가로구역 합계 옆에 **분할시설 면적**을 별도로 표시하도록 보완했다.

## 4. 검증
- `python -m py_compile app.py` PASS
- `node --check` 인라인 JS PASS
- 회귀검사: `r9 refinement placement`까지 전부 PASS
- 이후 전체 회귀검사는 기존 기준본에 없는 공식 PDF를 요구하는 `r10 scheme fail-safe`에서 중단. 이번 수정과 무관.
- 신규 정적 회귀조건:
  - `projectBoundaryRelation()` 존재
  - 외곽도로 경계부 비율 `>= 0.55`
  - `내부 관통도로 사업구역 포함` 원칙 존재
  - 사업구역 생성부에서 `polygonOuterShellOnly(merged)` 사용 금지

### 합성 공간검증
100m×100m 검토요청지 기준:
- 상단을 따라가는 8m 도로 → 경계부 비율 약 78% → 외곽도로
- 내부를 가로지르는 6m 도로 → 경계부 비율 약 12% → 내부 관통도로

따라서 두 도로를 구분하는 방향으로 작동한다.

## 5. 아직 미연결
이 빌드는 여전히 **범위 추출 검증용**이다.
- 사업구역을 전체 사업판정 기준으로 승격하지 않음
- 가로구역을 역세권 Rule에 아직 연결하지 않음

실제 사례지에서 `사업구역 1개 / 가로구역 2개`가 확인된 다음 연결한다.
