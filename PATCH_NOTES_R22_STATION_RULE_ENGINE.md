# R22 Station Rule Engine patch notes

## 목적
이 패치는 이전 `urban-strategy-v2.5.0-r22-station-fact-engine.zip`의 불완전한 수정사항을 대체한다.
이전 패치는 1km 주변역 Fact와 일부 보조함수를 추가했지만 여러 사업모듈이 여전히 `최근접역(c.dist)` 중심의 기존 판정식을 사용했고, 화면도 판정역을 확인하기 어려웠다.
이번 패치는 **공통 다중역 Fact를 실제 사업별 Rule에 연결**하는 데 초점을 둔다.

앱 내부 버전은 사용자 원칙에 따라 `v2.5.0`을 유지한다.

## 공통 역세권 Fact
- 대상지 centroid 기준 1,000m 안의 모든 역사 후보를 수집한다. 1,000m는 후보검색 범위이며 법정 판정거리가 아니다.
- 각 역사별로 대상지 경계 최단거리와 250m/350m/500m 대상지 중첩면적·포함률·전체포함 여부를 저장한다.
- 동일 물리역은 노선정보를 합치되, 같은 이름의 서로 떨어진 역을 잘못 합쳐 가짜 환승역으로 만드는 것을 방지한다.
- 최근접역은 참고값으로만 유지하고 사업 판정역은 각 사업 Rule이 후보역 전체에서 선택한다.

## 노선·환승 Fact 보강
- `/api/reference/station-lines`를 추가했다.
- 기존 서울 열린데이터광장 인증키 설정을 이용해 서울 지하철 운행노선 정보를 보강한다.
- 공식 역명에 붙는 괄호형 부역명(예: `왕십리(성동구청)`)을 정규화해 역사 geometry와 매칭한다.
- 공식 노선정보가 없으면 환승 여부를 자동확정하지 않고 REVIEW로 남긴다.

## 사업별 실제 판정식 연결

### 역세권활성화
- 후보역 전체를 검사한다.
- 역별로 250m/350m 적용반경을 결정한다.
  - 환승역 또는 중심지 조건 충족 시 350m 경로
  - 공식 노선자료로 비환승이 확인되는 경우 250m 경로
  - 노선정보 미확정이면 보수적으로 350m 후보를 보되 자동 PASS 금지
- `역세권 범위가 가로구역의 1/2 이상 포함` 조건을 역별로 계산한다.
- 현재 내장 기초단위구/도로폭 기반 추정블록은 공식 가로구역으로 간주하지 않으며 자동 PASS하지 않는다.

### 성장잠재권활성화
- 역세권활성화 공간대상 여부를 선행 배타게이트로 둔다.
- 역세권활성화 대상이 공식 확인되면 성장잠재권은 비활성화한다.
- 가로구역이 미확정이면 성장잠재권도 HOLD/REVIEW한다.
- 간선도로 기능자료가 아직 공식 확인되지 않으면 폭원만으로 PASS하지 않는다.

### 안심주택
- 최근접역 거리로 판정하지 않는다.
- 각 역별 250m 대상지 포함률을 검사하여 하나의 역 기준 50% 이상이면 일반경로 PASS.
- 250m 포함률 50% 미만은 추가 공공기여/통합심의 REVIEW.
- 350m는 예외지정 심의경로로만 REVIEW하며 350m 포함률도 역별로 계산한다.

### 역세권복합개발(고밀주거)
- 두 개의 가로구역 조건을 분리했다.
  1. 한 역의 250m 역세권이 해당 가로구역을 얼마나 포함하는지
  2. 사업대상지가 해당 가로구역을 얼마나 점유하는지
- 공식 가로구역이 없으면 자동 PASS하지 않고 REVIEW한다.
- 250m 밖이라는 이유만으로 임의 완화 PASS하던 경로를 제거했다.

### 역세권 장기전세
- 후보역 전체에서 한 개 역 기준 350m/500m 전체포함 조건을 검사한다.
- 여러 역의 버퍼 합집합으로 전체포함을 대체하지 않는다.
- 700% 특례의 역세권 조건도 별도 판정역을 사용한다.

### 도심공공주택복합
- 주거상업고밀형은 각 역의 350m 대상지 포함률을 비교해 판정역을 선택한다.
- 준공업 특례의 역세권 조건도 후보역 전체를 검사한다.

### 도심복합개발 혁신지구
- 성장거점형: 후보역 전체에서 2개 이상 철도노선 환승결절지를 찾아 500m 조건을 검사한다. 최근접 단일노선역은 결절지 판정역이 아니다.
- 주거중심형: 한 역 기준 350m/500m 대상지 포함률을 검사하며 `50% 이상`을 기준으로 처리한다.
- 상세검토서에 실제 환승결절 판정역 또는 350/500m 판정역을 표시한다.

### 소규모재개발 및 역세권 용적률 특례
- `c.dist` 최근접역 고정 대신 후보역 전체에서 적격역을 선택한다.
- 역세권 Fact가 로드되지 않은 경우에만 기존 수기거리값을 REVIEW용 fallback으로 사용한다.

## UI
- 역세권 현황에 1km 후보역 전체를 표 형태로 표시한다.
- 각 역별 거리, 노선/환승, 250m/350m/500m 대상지 포함률을 표시한다.
- 최근접역은 참고정보이고 사업별 판정역과 구분한다.
- 도심복합개발 성장거점형 상세검토서에는 `환승결절 판정역`을 표시한다.

## 아직 자동확정하지 않는 항목
- 공식 가로구역 geometry
- 공식 간선도로 기능/등급

향후 행안부 주소기반 기초단위구·도로기능 자료가 제공되면 현재 Fact 인터페이스에 연결해 자동판정하도록 설계했다. 그 전까지는 추정블록 및 도로폭만으로 법정 가로구역/간선도로 요건을 자동 PASS하지 않는다.

## 검증
- JavaScript syntax: `node --check` PASS
- Python syntax: `python -m py_compile app.py` PASS
- `regression_checks.py`: `v2.5.0 regression checks: PASS`
- FastAPI TestClient: `/health`, `/api/reference/stations`, `/api/reference/station-lines` 모두 HTTP 200 확인
