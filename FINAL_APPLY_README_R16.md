# R16 적용안내 — 도시정비형 연계판정 + 가로주택 seed/가로구역 판정 + REVIEW/조건부 분리

## 기준
- 프론트 기준: R14 `app.html`
- 백엔드 기준: R15 `app.py`
- R16 실제 수정파일: **app.html 1개**
- R15 `app.py`는 변경하지 않음. SHA256: `e4f93543dd428d62c2ae9c19fbc552d982b5573f239347e818bf4950e92d9d64`

## 적용
현재 저장소의 `app.html`만 R16 `app.html`로 교체한다.
`app.py`, 도로/기초단위구/역세권/정비구역 등 기존 데이터 파일은 삭제·교체하지 않는다.

## 주요 변경
1. `REVIEW`를 화면에서 `조건부 검토`로 번역하던 공통 UI를 `확인필요`로 수정.
   - 실제 제도상 예외·심의경로는 `conditional=true`일 때만 조건부 표현.
2. 가로주택정비 가로구역 판정:
   - 사용자 검토구역은 가로구역 경계가 아니라 seed로 사용.
   - 가로구역 후보 선택 시 `가로구역 점유율`보다 `검토구역 편입률`을 우선.
   - 하나의 후보 가로구역이 검토구역을 사실상 전부 포함하면 다른 대안 후보가 존재해도 `multi_block`만으로 탈락시키지 않음.
   - 화면에 `검토구역 편입률`과 `가로구역 점유율`을 분리 표시.
   - 내부 98% 값은 법정기준이 아니라 GIS topology tolerance로만 사용.
3. 역세권활성화:
   - 대표(selected) 추진방식과 적용가능(available) 추진방식을 분리.
   - 지구단위계획/도시정비형 재개발 방식을 별도로 평가·표시.
4. 도시정비형 재개발:
   - `독립추진`과 `정책사업 연계추진`을 분리.
   - 역세권활성화가 도시정비형 재개발 방식을 사용할 수 있으면 대표경로가 지구단위계획이어도 연계 가능성을 보존.
   - 독립경로 FAIL/REVIEW가 정책사업 연계 PASS를 강제로 죽이지 않음.
   - 후보카드에 `독립추진 가능` / `연계추진 가능` / `독립·연계 가능`을 구분 표시.
5. 14개 도시정비형 정비가능구역:
   - 공식 경계 GIS는 이번 패치에서 새로 만들지 않음.
   - 공식 경계가 없는 상태에서 중심지명만으로 자동 PASS하지 않는 기존 보수원칙 유지.
   - 대신 정책사업 연계경로가 확인되면 도시정비형 전체가 불필요하게 REVIEW로 묻히지 않도록 수정.

## 별도 후속과제
- 소규모재개발 8m/4m 접도 FACT 정교화.
- 도시정비형 재개발 14개 정비가능구역 공식 경계 GIS 구축·연결.
